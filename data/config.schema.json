{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "Config",
  "type": "object",
  "properties": {
    "appearance": {
      "$ref": "#/$defs/Appearance",
      "default": {
        "animation-speed": 1.0,
        "blur": [],
        "color-scheme": "auto",
        "theme": "adwaita",
        "theme-variant": ""
      }
    },
    "applets": {
      "type": "object",
      "properties": {
        "audio": {
          "description": "Output volume, with the default sink and per-application streams in its popover.",
          "type": "object",
          "properties": {
            "extends": {
              "type": "string",
              "const": "audio"
            },
            "settings-command": {
              "description": "The program that row runs, as a command and its arguments:\n`[\"xdg-open\", \"https://calendar.google.com/\"]`. It is a list rather than one string because\nthere is no shell between here and the program — an argument containing a space is one\nelement, and nothing has to be quoted, escaped or protected from word splitting.",
              "type": "array",
              "items": {
                "type": "string"
              }
            },
            "settings-label": {
              "description": "The label on the row the applet's popover puts in its footer. Set it together with\n`settings-command`.",
              "type": [
                "string",
                "null"
              ]
            },
            "tooltip-format": {
              "description": "What the bar's tooltip reads. The tokens are the applet's own — `strftime` for the clock,\n`{index}` and `{name}` for the pager, `{summary}`, `{detail}`, `{when}` and `{conflicts}`\nfor next-event — the same way `label` already differs between them. A token with nothing\nbehind it renders as nothing. Unset means the applet shows no tooltip.",
              "type": [
                "string",
                "null"
              ]
            }
          },
          "additionalProperties": false,
          "required": []
        },
        "battery": {
          "description": "Charge level and time remaining, with the power profile in its popover.",
          "type": "object",
          "properties": {
            "extends": {
              "type": "string",
              "const": "battery"
            },
            "indicator-style": {
              "$ref": "#/$defs/BatteryIndicatorStyle",
              "default": "icon-only"
            },
            "label-format": {
              "type": "string",
              "default": "{percentage}"
            },
            "settings-command": {
              "description": "The program that row runs, as a command and its arguments:\n`[\"xdg-open\", \"https://calendar.google.com/\"]`. It is a list rather than one string because\nthere is no shell between here and the program — an argument containing a space is one\nelement, and nothing has to be quoted, escaped or protected from word splitting.",
              "type": "array",
              "items": {
                "type": "string"
              }
            },
            "settings-label": {
              "description": "The label on the row the applet's popover puts in its footer. Set it together with\n`settings-command`.",
              "type": [
                "string",
                "null"
              ]
            },
            "tooltip-format": {
              "description": "What the bar's tooltip reads. The tokens are the applet's own — `strftime` for the clock,\n`{index}` and `{name}` for the pager, `{summary}`, `{detail}`, `{when}` and `{conflicts}`\nfor next-event — the same way `label` already differs between them. A token with nothing\nbehind it renders as nothing. Unset means the applet shows no tooltip.",
              "type": [
                "string",
                "null"
              ]
            }
          },
          "additionalProperties": false,
          "required": []
        },
        "bluetooth": {
          "description": "Adapter state and paired devices.",
          "type": "object",
          "properties": {
            "devices": {
              "description": "How many paired devices the popover lists before the rest go behind a drawer.",
              "type": "integer",
              "format": "uint",
              "default": 6,
              "minimum": 0
            },
            "extends": {
              "type": "string",
              "const": "bluetooth"
            },
            "nearby": {
              "description": "How many nearby devices the popover lists while scanning.",
              "type": "integer",
              "format": "uint",
              "default": 8,
              "minimum": 0
            },
            "settings-command": {
              "description": "The program that row runs, as a command and its arguments:\n`[\"xdg-open\", \"https://calendar.google.com/\"]`. It is a list rather than one string because\nthere is no shell between here and the program — an argument containing a space is one\nelement, and nothing has to be quoted, escaped or protected from word splitting.",
              "type": "array",
              "items": {
                "type": "string"
              }
            },
            "settings-label": {
              "description": "The label on the row the applet's popover puts in its footer. Set it together with\n`settings-command`.",
              "type": [
                "string",
                "null"
              ]
            },
            "tooltip-format": {
              "description": "What the bar's tooltip reads. The tokens are the applet's own — `strftime` for the clock,\n`{index}` and `{name}` for the pager, `{summary}`, `{detail}`, `{when}` and `{conflicts}`\nfor next-event — the same way `label` already differs between them. A token with nothing\nbehind it renders as nothing. Unset means the applet shows no tooltip.",
              "type": [
                "string",
                "null"
              ]
            }
          },
          "additionalProperties": false,
          "required": []
        },
        "brightness": {
          "description": "Display backlight level, and the keyboard's own where the machine has one.",
          "type": "object",
          "properties": {
            "extends": {
              "type": "string",
              "const": "brightness"
            },
            "scroll-step": {
              "description": "How much a scroll notch on the chip moves the current display, in percent of its range.\nConverted to native units at the call site and rounded away from zero, so a notch always\nmoves something even on a display with a small native range. Zero would leave the wheel\nsilently dead; the range is 1 to 100.",
              "type": "integer",
              "format": "uint8",
              "default": 5,
              "maximum": 100,
              "minimum": 1
            },
            "settings-command": {
              "description": "The program that row runs, as a command and its arguments:\n`[\"xdg-open\", \"https://calendar.google.com/\"]`. It is a list rather than one string because\nthere is no shell between here and the program — an argument containing a space is one\nelement, and nothing has to be quoted, escaped or protected from word splitting.",
              "type": "array",
              "items": {
                "type": "string"
              }
            },
            "settings-label": {
              "description": "The label on the row the applet's popover puts in its footer. Set it together with\n`settings-command`.",
              "type": [
                "string",
                "null"
              ]
            },
            "show-keyboard": {
              "description": "Whether the keyboard's own backlight, where the machine has one, gets a fader in the\npopover alongside the displays.",
              "type": "boolean",
              "default": true
            },
            "tooltip-format": {
              "description": "What the bar's tooltip reads. The tokens are the applet's own — `strftime` for the clock,\n`{index}` and `{name}` for the pager, `{summary}`, `{detail}`, `{when}` and `{conflicts}`\nfor next-event — the same way `label` already differs between them. A token with nothing\nbehind it renders as nothing. Unset means the applet shows no tooltip.",
              "type": [
                "string",
                "null"
              ]
            }
          },
          "additionalProperties": false,
          "required": []
        },
        "clipboard": {
          "description": "Clipboard history.",
          "type": "object",
          "properties": {
            "extends": {
              "type": "string",
              "const": "clipboard"
            },
            "label-format": {
              "description": "What the chip reads beside its icon. `{count}` is the number of entries held. Left unset the\nchip is an icon alone, which is what every applet does unless asked otherwise.",
              "type": [
                "string",
                "null"
              ]
            },
            "preview-chars": {
              "description": "How much of an entry a row shows before it is ellipsized, in characters. A row holds 128 at\nmost and shows roughly 24 before ellipsizing, so this trades tooltip detail for nothing much\non screen. Clamped to 8..=128.",
              "type": "integer",
              "format": "uint",
              "default": 72,
              "minimum": 0
            },
            "settings-command": {
              "description": "The program that row runs, as a command and its arguments:\n`[\"xdg-open\", \"https://calendar.google.com/\"]`. It is a list rather than one string because\nthere is no shell between here and the program — an argument containing a space is one\nelement, and nothing has to be quoted, escaped or protected from word splitting.",
              "type": "array",
              "items": {
                "type": "string"
              }
            },
            "settings-label": {
              "description": "The label on the row the applet's popover puts in its footer. Set it together with\n`settings-command`.",
              "type": [
                "string",
                "null"
              ]
            },
            "show-when-empty": {
              "description": "Whether the chip is shown when nothing has been copied yet. Off, the applet takes no room\nuntil there is something to open it for.",
              "type": "boolean",
              "default": false
            },
            "tooltip-format": {
              "description": "What the bar's tooltip reads. The tokens are the applet's own — `strftime` for the clock,\n`{index}` and `{name}` for the pager, `{summary}`, `{detail}`, `{when}` and `{conflicts}`\nfor next-event — the same way `label` already differs between them. A token with nothing\nbehind it renders as nothing. Unset means the applet shows no tooltip.",
              "type": [
                "string",
                "null"
              ]
            },
            "visible": {
              "description": "How many recent entries the popover lists. Nothing in the panel scrolls, so this is what\nkeeps the card on the screen. Pinned entries are listed above and are not counted here —\na pin is an explicit choice and is never dropped from the list. Clamped to 1..=50.",
              "type": "integer",
              "format": "uint",
              "default": 10,
              "minimum": 0
            }
          },
          "additionalProperties": false,
          "required": []
        },
        "clock": {
          "description": "The time and date, with a calendar in its popover.",
          "type": "object",
          "properties": {
            "extends": {
              "type": "string",
              "const": "clock"
            },
            "first-day": {
              "description": "Which day a calendar week starts on.",
              "$ref": "#/$defs/FirstDay",
              "default": "monday"
            },
            "hide-all-day": {
              "description": "Whether an all-day entry is left out of the calendar popover's day list entirely, rather\nthan shown alongside the day's timed events.",
              "type": "boolean",
              "default": false
            },
            "label-format": {
              "description": "A `strftime` format string for the bar, such as `%H:%M` or `%a %d %b %H:%M`.",
              "type": "string",
              "default": "%a, %-d %b, %H:%M"
            },
            "settings-command": {
              "description": "The program that row runs, as a command and its arguments:\n`[\"xdg-open\", \"https://calendar.google.com/\"]`. It is a list rather than one string because\nthere is no shell between here and the program — an argument containing a space is one\nelement, and nothing has to be quoted, escaped or protected from word splitting.",
              "type": "array",
              "items": {
                "type": "string"
              }
            },
            "settings-label": {
              "description": "The label on the row the applet's popover puts in its footer. Set it together with\n`settings-command`.",
              "type": [
                "string",
                "null"
              ]
            },
            "timezone": {
              "description": "The IANA zone this clock reads, such as `UTC` or `Asia/Tokyo`. Unset is the local zone;\nnaming one is how a second `[applets.clock-utc]` shows somewhere else.",
              "type": [
                "string",
                "null"
              ]
            },
            "timezones": {
              "description": "The other zones the popover lists under its world clock. Empty hides the section.",
              "type": "array",
              "default": [],
              "items": {
                "$ref": "#/$defs/Timezone"
              }
            },
            "tooltip-format": {
              "description": "What the bar's tooltip reads. The tokens are the applet's own — `strftime` for the clock,\n`{index}` and `{name}` for the pager, `{summary}`, `{detail}`, `{when}` and `{conflicts}`\nfor next-event — the same way `label` already differs between them. A token with nothing\nbehind it renders as nothing. Unset means the applet shows no tooltip.",
              "type": [
                "string",
                "null"
              ]
            },
            "week-numbers": {
              "description": "Whether the popover names the ISO week the shown month belongs to.",
              "type": "boolean",
              "default": true
            }
          },
          "additionalProperties": false,
          "required": []
        },
        "color-picker": {
          "description": "The latest picked color. A left click opens the lens to pick one from the screen, a right\nclick the palette of recent picks in every notation. Needs `glimpse-picker`.",
          "type": "object",
          "properties": {
            "extends": {
              "type": "string",
              "const": "color-picker"
            },
            "settings-command": {
              "description": "The program that row runs, as a command and its arguments:\n`[\"xdg-open\", \"https://calendar.google.com/\"]`. It is a list rather than one string because\nthere is no shell between here and the program — an argument containing a space is one\nelement, and nothing has to be quoted, escaped or protected from word splitting.",
              "type": "array",
              "items": {
                "type": "string"
              }
            },
            "settings-label": {
              "description": "The label on the row the applet's popover puts in its footer. Set it together with\n`settings-command`.",
              "type": [
                "string",
                "null"
              ]
            },
            "tooltip-format": {
              "description": "What the bar's tooltip reads. The tokens are the applet's own — `strftime` for the clock,\n`{index}` and `{name}` for the pager, `{summary}`, `{detail}`, `{when}` and `{conflicts}`\nfor next-event — the same way `label` already differs between them. A token with nothing\nbehind it renders as nothing. Unset means the applet shows no tooltip.",
              "type": [
                "string",
                "null"
              ]
            }
          },
          "additionalProperties": false,
          "required": []
        },
        "command": {
          "description": "A chip the user defines, running a program on a click or a scroll.",
          "type": "object",
          "properties": {
            "extends": {
              "type": "string",
              "const": "command"
            },
            "icon": {
              "description": "A themed icon name, or the absolute path to an image. Prefer a `-symbolic` name, which\nfollows the bar's color.",
              "type": [
                "string",
                "null"
              ]
            },
            "label": {
              "description": "Text beside the icon. With neither an icon nor a label the chip takes no room.",
              "type": [
                "string",
                "null"
              ]
            },
            "on-click": {
              "description": "What a left click runs.",
              "type": "array",
              "items": {
                "type": "string"
              }
            },
            "on-middle-click": {
              "description": "What a middle click runs.",
              "type": "array",
              "items": {
                "type": "string"
              }
            },
            "on-right-click": {
              "description": "What a right click runs.",
              "type": "array",
              "items": {
                "type": "string"
              }
            },
            "on-scroll-down": {
              "description": "What each notch of scrolling down runs, once per notch.",
              "type": "array",
              "items": {
                "type": "string"
              }
            },
            "on-scroll-left": {
              "description": "What each notch of scrolling left runs, once per notch.",
              "type": "array",
              "items": {
                "type": "string"
              }
            },
            "on-scroll-right": {
              "description": "What each notch of scrolling right runs, once per notch.",
              "type": "array",
              "items": {
                "type": "string"
              }
            },
            "on-scroll-up": {
              "description": "What each notch of scrolling up runs, once per notch.",
              "type": "array",
              "items": {
                "type": "string"
              }
            },
            "settings-command": {
              "description": "The program that row runs, as a command and its arguments:\n`[\"xdg-open\", \"https://calendar.google.com/\"]`. It is a list rather than one string because\nthere is no shell between here and the program — an argument containing a space is one\nelement, and nothing has to be quoted, escaped or protected from word splitting.",
              "type": "array",
              "items": {
                "type": "string"
              }
            },
            "settings-label": {
              "description": "The label on the row the applet's popover puts in its footer. Set it together with\n`settings-command`.",
              "type": [
                "string",
                "null"
              ]
            },
            "tooltip-format": {
              "description": "What the bar's tooltip reads. The tokens are the applet's own — `strftime` for the clock,\n`{index}` and `{name}` for the pager, `{summary}`, `{detail}`, `{when}` and `{conflicts}`\nfor next-event — the same way `label` already differs between them. A token with nothing\nbehind it renders as nothing. Unset means the applet shows no tooltip.",
              "type": [
                "string",
                "null"
              ]
            }
          },
          "additionalProperties": false,
          "required": []
        },
        "display": {
          "description": "Connected outputs, their modes and their arrangement.",
          "type": "object",
          "properties": {
            "extends": {
              "type": "string",
              "const": "display"
            },
            "settings-command": {
              "description": "The program that row runs, as a command and its arguments:\n`[\"xdg-open\", \"https://calendar.google.com/\"]`. It is a list rather than one string because\nthere is no shell between here and the program — an argument containing a space is one\nelement, and nothing has to be quoted, escaped or protected from word splitting.",
              "type": "array",
              "items": {
                "type": "string"
              }
            },
            "settings-label": {
              "description": "The label on the row the applet's popover puts in its footer. Set it together with\n`settings-command`.",
              "type": [
                "string",
                "null"
              ]
            },
            "tooltip-format": {
              "description": "What the bar's tooltip reads. The tokens are the applet's own — `strftime` for the clock,\n`{index}` and `{name}` for the pager, `{summary}`, `{detail}`, `{when}` and `{conflicts}`\nfor next-event — the same way `label` already differs between them. A token with nothing\nbehind it renders as nothing. Unset means the applet shows no tooltip.",
              "type": [
                "string",
                "null"
              ]
            }
          },
          "additionalProperties": false,
          "required": []
        },
        "exec": {
          "description": "Hosts a third-party applet binary that draws its own popover.",
          "type": "object",
          "properties": {
            "extends": {
              "type": "string",
              "const": "exec"
            },
            "settings-command": {
              "description": "The program that row runs, as a command and its arguments:\n`[\"xdg-open\", \"https://calendar.google.com/\"]`. It is a list rather than one string because\nthere is no shell between here and the program — an argument containing a space is one\nelement, and nothing has to be quoted, escaped or protected from word splitting.",
              "type": "array",
              "items": {
                "type": "string"
              }
            },
            "settings-label": {
              "description": "The label on the row the applet's popover puts in its footer. Set it together with\n`settings-command`.",
              "type": [
                "string",
                "null"
              ]
            },
            "tooltip-format": {
              "description": "What the bar's tooltip reads. The tokens are the applet's own — `strftime` for the clock,\n`{index}` and `{name}` for the pager, `{summary}`, `{detail}`, `{when}` and `{conflicts}`\nfor next-event — the same way `label` already differs between them. A token with nothing\nbehind it renders as nothing. Unset means the applet shows no tooltip.",
              "type": [
                "string",
                "null"
              ]
            }
          },
          "additionalProperties": false,
          "required": []
        },
        "heartbeat": {
          "description": "A counter that ticks once a second. A development fixture: it proves the service is\nreachable and events are arriving, and is not meant for a real bar.",
          "type": "object",
          "properties": {
            "extends": {
              "type": "string",
              "const": "heartbeat"
            },
            "settings-command": {
              "description": "The program that row runs, as a command and its arguments:\n`[\"xdg-open\", \"https://calendar.google.com/\"]`. It is a list rather than one string because\nthere is no shell between here and the program — an argument containing a space is one\nelement, and nothing has to be quoted, escaped or protected from word splitting.",
              "type": "array",
              "items": {
                "type": "string"
              }
            },
            "settings-label": {
              "description": "The label on the row the applet's popover puts in its footer. Set it together with\n`settings-command`.",
              "type": [
                "string",
                "null"
              ]
            },
            "tooltip-format": {
              "description": "What the bar's tooltip reads. The tokens are the applet's own — `strftime` for the clock,\n`{index}` and `{name}` for the pager, `{summary}`, `{detail}`, `{when}` and `{conflicts}`\nfor next-event — the same way `label` already differs between them. A token with nothing\nbehind it renders as nothing. Unset means the applet shows no tooltip.",
              "type": [
                "string",
                "null"
              ]
            }
          },
          "additionalProperties": false,
          "required": []
        },
        "idle": {
          "description": "Idle inhibition, for keeping the screen awake.",
          "type": "object",
          "properties": {
            "extends": {
              "type": "string",
              "const": "idle"
            },
            "settings-command": {
              "description": "The program that row runs, as a command and its arguments:\n`[\"xdg-open\", \"https://calendar.google.com/\"]`. It is a list rather than one string because\nthere is no shell between here and the program — an argument containing a space is one\nelement, and nothing has to be quoted, escaped or protected from word splitting.",
              "type": "array",
              "items": {
                "type": "string"
              }
            },
            "settings-label": {
              "description": "The label on the row the applet's popover puts in its footer. Set it together with\n`settings-command`.",
              "type": [
                "string",
                "null"
              ]
            },
            "tooltip-format": {
              "description": "What the bar's tooltip reads. The tokens are the applet's own — `strftime` for the clock,\n`{index}` and `{name}` for the pager, `{summary}`, `{detail}`, `{when}` and `{conflicts}`\nfor next-event — the same way `label` already differs between them. A token with nothing\nbehind it renders as nothing. Unset means the applet shows no tooltip.",
              "type": [
                "string",
                "null"
              ]
            }
          },
          "additionalProperties": false,
          "required": []
        },
        "keyboard": {
          "description": "The active keyboard layout, and switches between the configured ones.",
          "type": "object",
          "properties": {
            "extends": {
              "type": "string",
              "const": "keyboard"
            },
            "settings-command": {
              "description": "The program that row runs, as a command and its arguments:\n`[\"xdg-open\", \"https://calendar.google.com/\"]`. It is a list rather than one string because\nthere is no shell between here and the program — an argument containing a space is one\nelement, and nothing has to be quoted, escaped or protected from word splitting.",
              "type": "array",
              "items": {
                "type": "string"
              }
            },
            "settings-label": {
              "description": "The label on the row the applet's popover puts in its footer. Set it together with\n`settings-command`.",
              "type": [
                "string",
                "null"
              ]
            },
            "tooltip-format": {
              "description": "What the bar's tooltip reads. The tokens are the applet's own — `strftime` for the clock,\n`{index}` and `{name}` for the pager, `{summary}`, `{detail}`, `{when}` and `{conflicts}`\nfor next-event — the same way `label` already differs between them. A token with nothing\nbehind it renders as nothing. Unset means the applet shows no tooltip.",
              "type": [
                "string",
                "null"
              ]
            }
          },
          "additionalProperties": false,
          "required": []
        },
        "mpris": {
          "description": "The currently playing track, with transport controls in its popover.",
          "type": "object",
          "properties": {
            "extends": {
              "type": "string",
              "const": "mpris"
            },
            "label-format": {
              "description": "What the bar shows. Placeholders are replaced by name: `{player}`, `{title}`, `{artist}`,\n`{album}`, `{state}`, `{position}`, `{duration}` and `{remaining}`. A placeholder with\nnothing behind it renders as nothing.",
              "type": "string",
              "default": "{title} — {artist}"
            },
            "max-length": {
              "description": "Longest label the bar shows, in characters; the rest is ellipsized. Track titles come from\nwhatever is playing and are unbounded, and a bar that resizes as they change is worse than\none that truncates.",
              "type": "integer",
              "format": "uint8",
              "default": 40,
              "maximum": 255,
              "minimum": 0
            },
            "settings-command": {
              "description": "The program that row runs, as a command and its arguments:\n`[\"xdg-open\", \"https://calendar.google.com/\"]`. It is a list rather than one string because\nthere is no shell between here and the program — an argument containing a space is one\nelement, and nothing has to be quoted, escaped or protected from word splitting.",
              "type": "array",
              "items": {
                "type": "string"
              }
            },
            "settings-label": {
              "description": "The label on the row the applet's popover puts in its footer. Set it together with\n`settings-command`.",
              "type": [
                "string",
                "null"
              ]
            },
            "show-art": {
              "description": "Whether the popover shows album art.",
              "type": "boolean",
              "default": true
            },
            "show-others": {
              "description": "Whether the popover lists the players other than the one on the bar.",
              "type": "boolean",
              "default": true
            },
            "tooltip-format": {
              "description": "What the bar's tooltip reads. The tokens are the applet's own — `strftime` for the clock,\n`{index}` and `{name}` for the pager, `{summary}`, `{detail}`, `{when}` and `{conflicts}`\nfor next-event — the same way `label` already differs between them. A token with nothing\nbehind it renders as nothing. Unset means the applet shows no tooltip.",
              "type": [
                "string",
                "null"
              ]
            }
          },
          "additionalProperties": false,
          "required": []
        },
        "network": {
          "description": "Connection state, with the available networks in its popover.",
          "type": "object",
          "properties": {
            "extends": {
              "type": "string",
              "const": "network"
            },
            "metered-in-tooltip": {
              "description": "Whether a metered connection is named in the tooltip. It is always marked on the row inside\nthe popover; this is only about the bar.",
              "type": "boolean",
              "default": true
            },
            "settings-command": {
              "description": "The program that row runs, as a command and its arguments:\n`[\"xdg-open\", \"https://calendar.google.com/\"]`. It is a list rather than one string because\nthere is no shell between here and the program — an argument containing a space is one\nelement, and nothing has to be quoted, escaped or protected from word splitting.",
              "type": "array",
              "items": {
                "type": "string"
              }
            },
            "settings-label": {
              "description": "The label on the row the applet's popover puts in its footer. Set it together with\n`settings-command`.",
              "type": [
                "string",
                "null"
              ]
            },
            "tooltip-format": {
              "description": "What the bar's tooltip reads. The tokens are the applet's own — `strftime` for the clock,\n`{index}` and `{name}` for the pager, `{summary}`, `{detail}`, `{when}` and `{conflicts}`\nfor next-event — the same way `label` already differs between them. A token with nothing\nbehind it renders as nothing. Unset means the applet shows no tooltip.",
              "type": [
                "string",
                "null"
              ]
            },
            "visible-networks": {
              "description": "How many networks the popover lists before the rest go behind a \"more networks\" row. `0`\nlists every network in range.",
              "type": "integer",
              "format": "uint",
              "default": 8,
              "minimum": 0
            },
            "vpn-chip": {
              "description": "Whether an active VPN gets a chip of its own beside the network chip. An indicator is an\nicon, and two states overlaid on one glyph are illegible at bar size.",
              "type": "boolean",
              "default": true
            }
          },
          "additionalProperties": false,
          "required": []
        },
        "next-event": {
          "description": "The next entry from the configured calendars.",
          "type": "object",
          "properties": {
            "all-day": {
              "description": "Whether an all-day entry may take the bar. A timed event always wins over one, so this\nonly decides what happens on a day holding nothing else — leaving it off keeps a week of\nleave from pinning the applet open for the whole week.",
              "type": "boolean",
              "default": false
            },
            "countdown": {
              "description": "How close an event has to be before the bar spells out how long is left, in minutes. Inside\nit the indicator reads `Design review in 12 min`, or `ends in 25 min` once it has started;\noutside it the title stands alone. Set it below `within` to have an entry appear quietly and\nstart counting only as it approaches; `0` never counts.",
              "type": "integer",
              "format": "uint64",
              "default": 60,
              "minimum": 0
            },
            "extends": {
              "type": "string",
              "const": "next-event"
            },
            "horizon": {
              "description": "How far ahead the popover's list reaches, in minutes. This is the second of two windows and\nthe wider one: `within` decides when the bar lights up, `horizon` decides what the list\nholds once you open it. The default 720 is twelve hours, so a late-afternoon glance still\nreaches tomorrow morning — a plain \"today only\" rule empties the list at exactly the hour\ntomorrow's first meeting starts mattering. 1440 is a rolling day.",
              "type": "integer",
              "format": "uint64",
              "default": 720,
              "minimum": 0
            },
            "settings-command": {
              "description": "The program that row runs, as a command and its arguments:\n`[\"xdg-open\", \"https://calendar.google.com/\"]`. It is a list rather than one string because\nthere is no shell between here and the program — an argument containing a space is one\nelement, and nothing has to be quoted, escaped or protected from word splitting.",
              "type": "array",
              "items": {
                "type": "string"
              }
            },
            "settings-label": {
              "description": "The label on the row the applet's popover puts in its footer. Set it together with\n`settings-command`.",
              "type": [
                "string",
                "null"
              ]
            },
            "tooltip-format": {
              "description": "What the bar's tooltip reads. The tokens are the applet's own — `strftime` for the clock,\n`{index}` and `{name}` for the pager, `{summary}`, `{detail}`, `{when}` and `{conflicts}`\nfor next-event — the same way `label` already differs between them. A token with nothing\nbehind it renders as nothing. Unset means the applet shows no tooltip.",
              "type": [
                "string",
                "null"
              ]
            },
            "upcoming": {
              "description": "How many entries the popover lists under the one it is showing, whichever `horizon` leaves.\nCapped at 20, because a list longer than that is a popover taller than the screen.",
              "type": "integer",
              "format": "uint",
              "default": 5,
              "minimum": 0
            },
            "within": {
              "description": "How long before an event starts it takes the bar, in minutes. Anything further out than\nthis leaves the applet empty, which is what keeps a meeting two days away off a bar that\nhas nothing to say about it. An event that has already started stays until it ends,\nhowever long it has been running, so `0` shows only what is under way.",
              "type": "integer",
              "format": "uint64",
              "default": 60,
              "minimum": 0
            }
          },
          "additionalProperties": false,
          "required": []
        },
        "notifications": {
          "description": "Unread notifications, with their history in its popover.",
          "type": "object",
          "properties": {
            "extends": {
              "type": "string",
              "const": "notifications"
            },
            "indicator-style": {
              "$ref": "#/$defs/NotificationIndicatorStyle",
              "default": "icon-dot"
            },
            "settings-command": {
              "description": "The program that row runs, as a command and its arguments:\n`[\"xdg-open\", \"https://calendar.google.com/\"]`. It is a list rather than one string because\nthere is no shell between here and the program — an argument containing a space is one\nelement, and nothing has to be quoted, escaped or protected from word splitting.",
              "type": "array",
              "items": {
                "type": "string"
              }
            },
            "settings-label": {
              "description": "The label on the row the applet's popover puts in its footer. Set it together with\n`settings-command`.",
              "type": [
                "string",
                "null"
              ]
            },
            "tooltip-format": {
              "description": "What the bar's tooltip reads. The tokens are the applet's own — `strftime` for the clock,\n`{index}` and `{name}` for the pager, `{summary}`, `{detail}`, `{when}` and `{conflicts}`\nfor next-event — the same way `label` already differs between them. A token with nothing\nbehind it renders as nothing. Unset means the applet shows no tooltip.",
              "type": [
                "string",
                "null"
              ]
            }
          },
          "additionalProperties": false,
          "required": []
        },
        "pager": {
          "description": "A strip of workspaces or windows, one slot each, that switches between them on a click.",
          "type": "object",
          "properties": {
            "extends": {
              "type": "string",
              "const": "pager"
            },
            "focused-label": {
              "description": "The label for the slot the user is on, so the current workspace can show its name while\nthe rest stay numbers. Same tokens as `label`; unset falls back to it.",
              "type": [
                "string",
                "null"
              ]
            },
            "label": {
              "description": "What a slot reads, when the shape is `labels`, and the fallback for every state below.\nUnderstands `{index}`, `{id}`, `{name}`, `{name-or-index}` and `{workspace-name}`.\n`{index}` falls back to the id, because only niri numbers its workspaces separately from\ntheir ids; in `windows` mode it is the slot's position and `{name}` is the window's app\nid, which is why `{workspace-name}` exists.",
              "type": "string",
              "default": "{index}"
            },
            "mode": {
              "description": "Whether each slot is a workspace or a window.",
              "$ref": "#/$defs/PagerMode",
              "default": "workspaces"
            },
            "scope": {
              "description": "How much of the session the strip covers.",
              "$ref": "#/$defs/PagerScope",
              "default": "output"
            },
            "settings-command": {
              "description": "The program that row runs, as a command and its arguments:\n`[\"xdg-open\", \"https://calendar.google.com/\"]`. It is a list rather than one string because\nthere is no shell between here and the program — an argument containing a space is one\nelement, and nothing has to be quoted, escaped or protected from word splitting.",
              "type": "array",
              "items": {
                "type": "string"
              }
            },
            "settings-label": {
              "description": "The label on the row the applet's popover puts in its footer. Set it together with\n`settings-command`.",
              "type": [
                "string",
                "null"
              ]
            },
            "shape": {
              "description": "Whether a slot is drawn as a dot or as its label in a pill.",
              "$ref": "#/$defs/PagerShape",
              "default": "dots"
            },
            "tooltip-format": {
              "description": "What the bar's tooltip reads. The tokens are the applet's own — `strftime` for the clock,\n`{index}` and `{name}` for the pager, `{summary}`, `{detail}`, `{when}` and `{conflicts}`\nfor next-event — the same way `label` already differs between them. A token with nothing\nbehind it renders as nothing. Unset means the applet shows no tooltip.",
              "type": [
                "string",
                "null"
              ]
            },
            "unfocused-label": {
              "description": "The label for every slot the user is not on. Same tokens as `label`; unset falls back\nto it.",
              "type": [
                "string",
                "null"
              ]
            },
            "urgent-label": {
              "description": "The label for a slot asking for attention. Takes precedence over the other two, because a\nwindow wanting attention is the one thing the strip exists to surface. Same tokens as\n`label`; unset falls back to it.",
              "type": [
                "string",
                "null"
              ]
            }
          },
          "additionalProperties": false,
          "required": []
        },
        "places": {
          "description": "Home and user directories, bookmarks, network shares and the trash.",
          "type": "object",
          "properties": {
            "bookmarks": {
              "description": "How many bookmarks the popover lists before the rest go behind a drawer.",
              "type": "integer",
              "format": "uint",
              "default": 8,
              "minimum": 0
            },
            "extends": {
              "type": "string",
              "const": "places"
            },
            "settings-command": {
              "description": "The program that row runs, as a command and its arguments:\n`[\"xdg-open\", \"https://calendar.google.com/\"]`. It is a list rather than one string because\nthere is no shell between here and the program — an argument containing a space is one\nelement, and nothing has to be quoted, escaped or protected from word splitting.",
              "type": "array",
              "items": {
                "type": "string"
              }
            },
            "settings-label": {
              "description": "The label on the row the applet's popover puts in its footer. Set it together with\n`settings-command`.",
              "type": [
                "string",
                "null"
              ]
            },
            "tooltip-format": {
              "description": "What the bar's tooltip reads. The tokens are the applet's own — `strftime` for the clock,\n`{index}` and `{name}` for the pager, `{summary}`, `{detail}`, `{when}` and `{conflicts}`\nfor next-event — the same way `label` already differs between them. A token with nothing\nbehind it renders as nothing. Unset means the applet shows no tooltip.",
              "type": [
                "string",
                "null"
              ]
            }
          },
          "additionalProperties": false,
          "required": []
        },
        "printing": {
          "description": "Active print jobs.",
          "type": "object",
          "properties": {
            "extends": {
              "type": "string",
              "const": "printing"
            },
            "jobs": {
              "description": "Print jobs listed in the popover before older ones are hidden.",
              "type": "integer",
              "format": "uint",
              "default": 8,
              "minimum": 0
            },
            "settings-command": {
              "description": "The program that row runs, as a command and its arguments:\n`[\"xdg-open\", \"https://calendar.google.com/\"]`. It is a list rather than one string because\nthere is no shell between here and the program — an argument containing a space is one\nelement, and nothing has to be quoted, escaped or protected from word splitting.",
              "type": "array",
              "items": {
                "type": "string"
              }
            },
            "settings-label": {
              "description": "The label on the row the applet's popover puts in its footer. Set it together with\n`settings-command`.",
              "type": [
                "string",
                "null"
              ]
            },
            "tooltip-format": {
              "description": "What the bar's tooltip reads. The tokens are the applet's own — `strftime` for the clock,\n`{index}` and `{name}` for the pager, `{summary}`, `{detail}`, `{when}` and `{conflicts}`\nfor next-event — the same way `label` already differs between them. A token with nothing\nbehind it renders as nothing. Unset means the applet shows no tooltip.",
              "type": [
                "string",
                "null"
              ]
            }
          },
          "additionalProperties": false,
          "required": []
        },
        "privacy": {
          "description": "Shows when the microphone, camera or screen is in use.",
          "type": "object",
          "properties": {
            "extends": {
              "type": "string",
              "const": "privacy"
            },
            "settings-command": {
              "description": "The program that row runs, as a command and its arguments:\n`[\"xdg-open\", \"https://calendar.google.com/\"]`. It is a list rather than one string because\nthere is no shell between here and the program — an argument containing a space is one\nelement, and nothing has to be quoted, escaped or protected from word splitting.",
              "type": "array",
              "items": {
                "type": "string"
              }
            },
            "settings-label": {
              "description": "The label on the row the applet's popover puts in its footer. Set it together with\n`settings-command`.",
              "type": [
                "string",
                "null"
              ]
            },
            "show-camera": {
              "description": "Whether the camera is shown when in use.",
              "type": "boolean",
              "default": true
            },
            "show-location": {
              "description": "Whether location access is shown when in use.",
              "type": "boolean",
              "default": true
            },
            "show-microphone": {
              "description": "Whether the microphone is shown when in use.",
              "type": "boolean",
              "default": true
            },
            "show-screencast": {
              "description": "Whether screen capture is shown when in use.",
              "type": "boolean",
              "default": true
            },
            "tooltip-format": {
              "description": "What the bar's tooltip reads. The tokens are the applet's own — `strftime` for the clock,\n`{index}` and `{name}` for the pager, `{summary}`, `{detail}`, `{when}` and `{conflicts}`\nfor next-event — the same way `label` already differs between them. A token with nothing\nbehind it renders as nothing. Unset means the applet shows no tooltip.",
              "type": [
                "string",
                "null"
              ]
            }
          },
          "additionalProperties": false,
          "required": []
        },
        "removable": {
          "description": "Removable drives and their volumes, with mount, unmount and eject in its popover.",
          "type": "object",
          "properties": {
            "extends": {
              "type": "string",
              "const": "removable"
            },
            "settings-command": {
              "description": "The program that row runs, as a command and its arguments:\n`[\"xdg-open\", \"https://calendar.google.com/\"]`. It is a list rather than one string because\nthere is no shell between here and the program — an argument containing a space is one\nelement, and nothing has to be quoted, escaped or protected from word splitting.",
              "type": "array",
              "items": {
                "type": "string"
              }
            },
            "settings-label": {
              "description": "The label on the row the applet's popover puts in its footer. Set it together with\n`settings-command`.",
              "type": [
                "string",
                "null"
              ]
            },
            "tooltip-format": {
              "description": "What the bar's tooltip reads. The tokens are the applet's own — `strftime` for the clock,\n`{index}` and `{name}` for the pager, `{summary}`, `{detail}`, `{when}` and `{conflicts}`\nfor next-event — the same way `label` already differs between them. A token with nothing\nbehind it renders as nothing. Unset means the applet shows no tooltip.",
              "type": [
                "string",
                "null"
              ]
            },
            "volumes": {
              "description": "How many drives the popover lists before the rest go behind a drawer.",
              "type": "integer",
              "format": "uint",
              "default": 6,
              "minimum": 0
            }
          },
          "additionalProperties": false,
          "required": []
        },
        "ruler": {
          "description": "Measures on-screen pixel distances. A left click opens the lens to measure from the\nscreen. Needs `glimpse-ruler`.",
          "type": "object",
          "properties": {
            "extends": {
              "type": "string",
              "const": "ruler"
            },
            "settings-command": {
              "description": "The program that row runs, as a command and its arguments:\n`[\"xdg-open\", \"https://calendar.google.com/\"]`. It is a list rather than one string because\nthere is no shell between here and the program — an argument containing a space is one\nelement, and nothing has to be quoted, escaped or protected from word splitting.",
              "type": "array",
              "items": {
                "type": "string"
              }
            },
            "settings-label": {
              "description": "The label on the row the applet's popover puts in its footer. Set it together with\n`settings-command`.",
              "type": [
                "string",
                "null"
              ]
            },
            "tooltip-format": {
              "description": "What the bar's tooltip reads. The tokens are the applet's own — `strftime` for the clock,\n`{index}` and `{name}` for the pager, `{summary}`, `{detail}`, `{when}` and `{conflicts}`\nfor next-event — the same way `label` already differs between them. A token with nothing\nbehind it renders as nothing. Unset means the applet shows no tooltip.",
              "type": [
                "string",
                "null"
              ]
            }
          },
          "additionalProperties": false,
          "required": []
        },
        "session": {
          "description": "Log out, suspend, restart and shut down.",
          "type": "object",
          "properties": {
            "extends": {
              "type": "string",
              "const": "session"
            },
            "settings-command": {
              "description": "The program that row runs, as a command and its arguments:\n`[\"xdg-open\", \"https://calendar.google.com/\"]`. It is a list rather than one string because\nthere is no shell between here and the program — an argument containing a space is one\nelement, and nothing has to be quoted, escaped or protected from word splitting.",
              "type": "array",
              "items": {
                "type": "string"
              }
            },
            "settings-label": {
              "description": "The label on the row the applet's popover puts in its footer. Set it together with\n`settings-command`.",
              "type": [
                "string",
                "null"
              ]
            },
            "tooltip-format": {
              "description": "What the bar's tooltip reads. The tokens are the applet's own — `strftime` for the clock,\n`{index}` and `{name}` for the pager, `{summary}`, `{detail}`, `{when}` and `{conflicts}`\nfor next-event — the same way `label` already differs between them. A token with nothing\nbehind it renders as nothing. Unset means the applet shows no tooltip.",
              "type": [
                "string",
                "null"
              ]
            }
          },
          "additionalProperties": false,
          "required": []
        },
        "system-monitor": {
          "description": "Live CPU, RAM, swap, disk, network and (amdgpu) GPU load. Its backing service samples\nnothing at all unless this kind is actually placed on a panel — see `placed_kinds`.",
          "type": "object",
          "properties": {
            "chip-format": {
              "description": "How each chip's own label reads. `{name}` is the chip's own localized name (\"CPU\", \"RAM\",\n…); `{value}` is its reading (\"42%\" for a percentage chip, \"↓1.2 MB/s\" for network). An\nunrecognized token is left as literal text rather than silently emptied.",
              "type": "string",
              "default": "{name} {value}"
            },
            "chips": {
              "description": "Chips shown on the bar, in order. Anything sampled but left out here still appears in the\npopover — this list is panel real estate, not visibility. A repeated chip collapses to its\nfirst occurrence.",
              "type": "array",
              "default": [
                "cpu",
                "ram"
              ],
              "items": {
                "$ref": "#/$defs/Chip"
              }
            },
            "critical-percent": {
              "description": "Percent at and above which a reading turns `Severity::Error`. This codebase's `Severity`\nenum has no \"Critical\" variant — it is `Info`/`Warning`/`Error` only.",
              "type": "integer",
              "format": "uint8",
              "default": 95,
              "maximum": 100,
              "minimum": 1
            },
            "extends": {
              "type": "string",
              "const": "system-monitor"
            },
            "settings-command": {
              "description": "The program that row runs, as a command and its arguments:\n`[\"xdg-open\", \"https://calendar.google.com/\"]`. It is a list rather than one string because\nthere is no shell between here and the program — an argument containing a space is one\nelement, and nothing has to be quoted, escaped or protected from word splitting.",
              "type": "array",
              "items": {
                "type": "string"
              }
            },
            "settings-label": {
              "description": "The label on the row the applet's popover puts in its footer. Set it together with\n`settings-command`.",
              "type": [
                "string",
                "null"
              ]
            },
            "tooltip-format": {
              "description": "What the bar's tooltip reads. The tokens are the applet's own — `strftime` for the clock,\n`{index}` and `{name}` for the pager, `{summary}`, `{detail}`, `{when}` and `{conflicts}`\nfor next-event — the same way `label` already differs between them. A token with nothing\nbehind it renders as nothing. Unset means the applet shows no tooltip.",
              "type": [
                "string",
                "null"
              ]
            },
            "warn-percent": {
              "description": "Percent at and above which any percentage reading (CPU, RAM, swap, GPU usage/memory) turns\n`Severity::Warning`. Network has no percentage and is never colored.",
              "type": "integer",
              "format": "uint8",
              "default": 85,
              "maximum": 100,
              "minimum": 1
            }
          },
          "additionalProperties": false,
          "required": []
        },
        "tray": {
          "description": "The system tray: icons from applications that ask for one.",
          "type": "object",
          "properties": {
            "extends": {
              "type": "string",
              "const": "tray"
            },
            "hide": {
              "description": "Item ids never shown, matched against the application's own `Id`. That id survives an\napplication restart, which the bus name it happens to hold does not.",
              "type": "array",
              "default": [],
              "items": {
                "type": "string"
              }
            },
            "max-visible": {
              "description": "How many icons stay on the bar; the rest open from the chevron beside them. `0` keeps every\nicon on the bar and shows no chevron.",
              "type": "integer",
              "format": "uint8",
              "default": 0,
              "maximum": 255,
              "minimum": 0
            },
            "pin": {
              "description": "Item ids kept on the bar whatever the cap, in the order given. Anything not named here\nfollows in the order the items registered.",
              "type": "array",
              "default": [],
              "items": {
                "type": "string"
              }
            },
            "settings-command": {
              "description": "The program that row runs, as a command and its arguments:\n`[\"xdg-open\", \"https://calendar.google.com/\"]`. It is a list rather than one string because\nthere is no shell between here and the program — an argument containing a space is one\nelement, and nothing has to be quoted, escaped or protected from word splitting.",
              "type": "array",
              "items": {
                "type": "string"
              }
            },
            "settings-label": {
              "description": "The label on the row the applet's popover puts in its footer. Set it together with\n`settings-command`.",
              "type": [
                "string",
                "null"
              ]
            },
            "tooltip-format": {
              "description": "What the bar's tooltip reads. The tokens are the applet's own — `strftime` for the clock,\n`{index}` and `{name}` for the pager, `{summary}`, `{detail}`, `{when}` and `{conflicts}`\nfor next-event — the same way `label` already differs between them. A token with nothing\nbehind it renders as nothing. Unset means the applet shows no tooltip.",
              "type": [
                "string",
                "null"
              ]
            }
          },
          "additionalProperties": false,
          "required": []
        },
        "weather": {
          "description": "Current conditions, with the forecast in its popover.",
          "type": "object",
          "properties": {
            "days": {
              "description": "How many days the popover's list shows. The list starts at tomorrow — today is already\nthe hero, the strip and the details page, and repeating it as a row says nothing new.",
              "type": "integer",
              "format": "uint8",
              "default": 6,
              "maximum": 255,
              "minimum": 0
            },
            "extends": {
              "type": "string",
              "const": "weather"
            },
            "hours": {
              "description": "How many hours the popover's strip shows. The strip starts at the next hour — the hour\nstanding is already the hero.",
              "type": "integer",
              "format": "uint8",
              "default": 4,
              "maximum": 255,
              "minimum": 0
            },
            "label": {
              "description": "What the bar and the popover call this place. Unset means a fixed place reads as the pair\nit was given, and `here` as whatever the geolocation service resolved.",
              "type": [
                "string",
                "null"
              ]
            },
            "place": {
              "description": "Which place this applet shows.",
              "$ref": "#/$defs/Place",
              "default": {
                "at": "here"
              }
            },
            "settings-command": {
              "description": "The program that row runs, as a command and its arguments:\n`[\"xdg-open\", \"https://calendar.google.com/\"]`. It is a list rather than one string because\nthere is no shell between here and the program — an argument containing a space is one\nelement, and nothing has to be quoted, escaped or protected from word splitting.",
              "type": "array",
              "items": {
                "type": "string"
              }
            },
            "settings-label": {
              "description": "The label on the row the applet's popover puts in its footer. Set it together with\n`settings-command`.",
              "type": [
                "string",
                "null"
              ]
            },
            "tooltip-format": {
              "description": "What the bar's tooltip reads. The tokens are the applet's own — `strftime` for the clock,\n`{index}` and `{name}` for the pager, `{summary}`, `{detail}`, `{when}` and `{conflicts}`\nfor next-event — the same way `label` already differs between them. A token with nothing\nbehind it renders as nothing. Unset means the applet shows no tooltip.",
              "type": [
                "string",
                "null"
              ]
            }
          },
          "additionalProperties": false,
          "required": []
        },
        "workspace-name": {
          "description": "The current workspace's name, renamed from its popover.",
          "type": "object",
          "properties": {
            "extends": {
              "type": "string",
              "const": "workspace-name"
            },
            "settings-command": {
              "description": "The program that row runs, as a command and its arguments:\n`[\"xdg-open\", \"https://calendar.google.com/\"]`. It is a list rather than one string because\nthere is no shell between here and the program — an argument containing a space is one\nelement, and nothing has to be quoted, escaped or protected from word splitting.",
              "type": "array",
              "items": {
                "type": "string"
              }
            },
            "settings-label": {
              "description": "The label on the row the applet's popover puts in its footer. Set it together with\n`settings-command`.",
              "type": [
                "string",
                "null"
              ]
            },
            "tooltip-format": {
              "description": "What the bar's tooltip reads. The tokens are the applet's own — `strftime` for the clock,\n`{index}` and `{name}` for the pager, `{summary}`, `{detail}`, `{when}` and `{conflicts}`\nfor next-event — the same way `label` already differs between them. A token with nothing\nbehind it renders as nothing. Unset means the applet shows no tooltip.",
              "type": [
                "string",
                "null"
              ]
            }
          },
          "additionalProperties": false,
          "required": []
        }
      },
      "additionalProperties": {
        "description": "Which kind of applet this is, and the settings that kind alone understands.",
        "oneOf": [
          {
            "description": "Output volume, with the default sink and per-application streams in its popover.",
            "type": "object",
            "properties": {
              "extends": {
                "type": "string",
                "const": "audio"
              },
              "settings-command": {
                "description": "The program that row runs, as a command and its arguments:\n`[\"xdg-open\", \"https://calendar.google.com/\"]`. It is a list rather than one string because\nthere is no shell between here and the program — an argument containing a space is one\nelement, and nothing has to be quoted, escaped or protected from word splitting.",
                "type": "array",
                "items": {
                  "type": "string"
                }
              },
              "settings-label": {
                "description": "The label on the row the applet's popover puts in its footer. Set it together with\n`settings-command`.",
                "type": [
                  "string",
                  "null"
                ]
              },
              "tooltip-format": {
                "description": "What the bar's tooltip reads. The tokens are the applet's own — `strftime` for the clock,\n`{index}` and `{name}` for the pager, `{summary}`, `{detail}`, `{when}` and `{conflicts}`\nfor next-event — the same way `label` already differs between them. A token with nothing\nbehind it renders as nothing. Unset means the applet shows no tooltip.",
                "type": [
                  "string",
                  "null"
                ]
              }
            },
            "additionalProperties": false,
            "required": [
              "extends"
            ]
          },
          {
            "description": "Charge level and time remaining, with the power profile in its popover.",
            "type": "object",
            "properties": {
              "extends": {
                "type": "string",
                "const": "battery"
              },
              "indicator-style": {
                "$ref": "#/$defs/BatteryIndicatorStyle",
                "default": "icon-only"
              },
              "label-format": {
                "type": "string",
                "default": "{percentage}"
              },
              "settings-command": {
                "description": "The program that row runs, as a command and its arguments:\n`[\"xdg-open\", \"https://calendar.google.com/\"]`. It is a list rather than one string because\nthere is no shell between here and the program — an argument containing a space is one\nelement, and nothing has to be quoted, escaped or protected from word splitting.",
                "type": "array",
                "items": {
                  "type": "string"
                }
              },
              "settings-label": {
                "description": "The label on the row the applet's popover puts in its footer. Set it together with\n`settings-command`.",
                "type": [
                  "string",
                  "null"
                ]
              },
              "tooltip-format": {
                "description": "What the bar's tooltip reads. The tokens are the applet's own — `strftime` for the clock,\n`{index}` and `{name}` for the pager, `{summary}`, `{detail}`, `{when}` and `{conflicts}`\nfor next-event — the same way `label` already differs between them. A token with nothing\nbehind it renders as nothing. Unset means the applet shows no tooltip.",
                "type": [
                  "string",
                  "null"
                ]
              }
            },
            "additionalProperties": false,
            "required": [
              "extends"
            ]
          },
          {
            "description": "Adapter state and paired devices.",
            "type": "object",
            "properties": {
              "devices": {
                "description": "How many paired devices the popover lists before the rest go behind a drawer.",
                "type": "integer",
                "format": "uint",
                "default": 6,
                "minimum": 0
              },
              "extends": {
                "type": "string",
                "const": "bluetooth"
              },
              "nearby": {
                "description": "How many nearby devices the popover lists while scanning.",
                "type": "integer",
                "format": "uint",
                "default": 8,
                "minimum": 0
              },
              "settings-command": {
                "description": "The program that row runs, as a command and its arguments:\n`[\"xdg-open\", \"https://calendar.google.com/\"]`. It is a list rather than one string because\nthere is no shell between here and the program — an argument containing a space is one\nelement, and nothing has to be quoted, escaped or protected from word splitting.",
                "type": "array",
                "items": {
                  "type": "string"
                }
              },
              "settings-label": {
                "description": "The label on the row the applet's popover puts in its footer. Set it together with\n`settings-command`.",
                "type": [
                  "string",
                  "null"
                ]
              },
              "tooltip-format": {
                "description": "What the bar's tooltip reads. The tokens are the applet's own — `strftime` for the clock,\n`{index}` and `{name}` for the pager, `{summary}`, `{detail}`, `{when}` and `{conflicts}`\nfor next-event — the same way `label` already differs between them. A token with nothing\nbehind it renders as nothing. Unset means the applet shows no tooltip.",
                "type": [
                  "string",
                  "null"
                ]
              }
            },
            "additionalProperties": false,
            "required": [
              "extends"
            ]
          },
          {
            "description": "Display backlight level, and the keyboard's own where the machine has one.",
            "type": "object",
            "properties": {
              "extends": {
                "type": "string",
                "const": "brightness"
              },
              "scroll-step": {
                "description": "How much a scroll notch on the chip moves the current display, in percent of its range.\nConverted to native units at the call site and rounded away from zero, so a notch always\nmoves something even on a display with a small native range. Zero would leave the wheel\nsilently dead; the range is 1 to 100.",
                "type": "integer",
                "format": "uint8",
                "default": 5,
                "maximum": 100,
                "minimum": 1
              },
              "settings-command": {
                "description": "The program that row runs, as a command and its arguments:\n`[\"xdg-open\", \"https://calendar.google.com/\"]`. It is a list rather than one string because\nthere is no shell between here and the program — an argument containing a space is one\nelement, and nothing has to be quoted, escaped or protected from word splitting.",
                "type": "array",
                "items": {
                  "type": "string"
                }
              },
              "settings-label": {
                "description": "The label on the row the applet's popover puts in its footer. Set it together with\n`settings-command`.",
                "type": [
                  "string",
                  "null"
                ]
              },
              "show-keyboard": {
                "description": "Whether the keyboard's own backlight, where the machine has one, gets a fader in the\npopover alongside the displays.",
                "type": "boolean",
                "default": true
              },
              "tooltip-format": {
                "description": "What the bar's tooltip reads. The tokens are the applet's own — `strftime` for the clock,\n`{index}` and `{name}` for the pager, `{summary}`, `{detail}`, `{when}` and `{conflicts}`\nfor next-event — the same way `label` already differs between them. A token with nothing\nbehind it renders as nothing. Unset means the applet shows no tooltip.",
                "type": [
                  "string",
                  "null"
                ]
              }
            },
            "additionalProperties": false,
            "required": [
              "extends"
            ]
          },
          {
            "description": "Clipboard history.",
            "type": "object",
            "properties": {
              "extends": {
                "type": "string",
                "const": "clipboard"
              },
              "label-format": {
                "description": "What the chip reads beside its icon. `{count}` is the number of entries held. Left unset the\nchip is an icon alone, which is what every applet does unless asked otherwise.",
                "type": [
                  "string",
                  "null"
                ]
              },
              "preview-chars": {
                "description": "How much of an entry a row shows before it is ellipsized, in characters. A row holds 128 at\nmost and shows roughly 24 before ellipsizing, so this trades tooltip detail for nothing much\non screen. Clamped to 8..=128.",
                "type": "integer",
                "format": "uint",
                "default": 72,
                "minimum": 0
              },
              "settings-command": {
                "description": "The program that row runs, as a command and its arguments:\n`[\"xdg-open\", \"https://calendar.google.com/\"]`. It is a list rather than one string because\nthere is no shell between here and the program — an argument containing a space is one\nelement, and nothing has to be quoted, escaped or protected from word splitting.",
                "type": "array",
                "items": {
                  "type": "string"
                }
              },
              "settings-label": {
                "description": "The label on the row the applet's popover puts in its footer. Set it together with\n`settings-command`.",
                "type": [
                  "string",
                  "null"
                ]
              },
              "show-when-empty": {
                "description": "Whether the chip is shown when nothing has been copied yet. Off, the applet takes no room\nuntil there is something to open it for.",
                "type": "boolean",
                "default": false
              },
              "tooltip-format": {
                "description": "What the bar's tooltip reads. The tokens are the applet's own — `strftime` for the clock,\n`{index}` and `{name}` for the pager, `{summary}`, `{detail}`, `{when}` and `{conflicts}`\nfor next-event — the same way `label` already differs between them. A token with nothing\nbehind it renders as nothing. Unset means the applet shows no tooltip.",
                "type": [
                  "string",
                  "null"
                ]
              },
              "visible": {
                "description": "How many recent entries the popover lists. Nothing in the panel scrolls, so this is what\nkeeps the card on the screen. Pinned entries are listed above and are not counted here —\na pin is an explicit choice and is never dropped from the list. Clamped to 1..=50.",
                "type": "integer",
                "format": "uint",
                "default": 10,
                "minimum": 0
              }
            },
            "additionalProperties": false,
            "required": [
              "extends"
            ]
          },
          {
            "description": "The time and date, with a calendar in its popover.",
            "type": "object",
            "properties": {
              "extends": {
                "type": "string",
                "const": "clock"
              },
              "first-day": {
                "description": "Which day a calendar week starts on.",
                "$ref": "#/$defs/FirstDay",
                "default": "monday"
              },
              "hide-all-day": {
                "description": "Whether an all-day entry is left out of the calendar popover's day list entirely, rather\nthan shown alongside the day's timed events.",
                "type": "boolean",
                "default": false
              },
              "label-format": {
                "description": "A `strftime` format string for the bar, such as `%H:%M` or `%a %d %b %H:%M`.",
                "type": "string",
                "default": "%a, %-d %b, %H:%M"
              },
              "settings-command": {
                "description": "The program that row runs, as a command and its arguments:\n`[\"xdg-open\", \"https://calendar.google.com/\"]`. It is a list rather than one string because\nthere is no shell between here and the program — an argument containing a space is one\nelement, and nothing has to be quoted, escaped or protected from word splitting.",
                "type": "array",
                "items": {
                  "type": "string"
                }
              },
              "settings-label": {
                "description": "The label on the row the applet's popover puts in its footer. Set it together with\n`settings-command`.",
                "type": [
                  "string",
                  "null"
                ]
              },
              "timezone": {
                "description": "The IANA zone this clock reads, such as `UTC` or `Asia/Tokyo`. Unset is the local zone;\nnaming one is how a second `[applets.clock-utc]` shows somewhere else.",
                "type": [
                  "string",
                  "null"
                ]
              },
              "timezones": {
                "description": "The other zones the popover lists under its world clock. Empty hides the section.",
                "type": "array",
                "default": [],
                "items": {
                  "$ref": "#/$defs/Timezone"
                }
              },
              "tooltip-format": {
                "description": "What the bar's tooltip reads. The tokens are the applet's own — `strftime` for the clock,\n`{index}` and `{name}` for the pager, `{summary}`, `{detail}`, `{when}` and `{conflicts}`\nfor next-event — the same way `label` already differs between them. A token with nothing\nbehind it renders as nothing. Unset means the applet shows no tooltip.",
                "type": [
                  "string",
                  "null"
                ]
              },
              "week-numbers": {
                "description": "Whether the popover names the ISO week the shown month belongs to.",
                "type": "boolean",
                "default": true
              }
            },
            "additionalProperties": false,
            "required": [
              "extends"
            ]
          },
          {
            "description": "The latest picked color. A left click opens the lens to pick one from the screen, a right\nclick the palette of recent picks in every notation. Needs `glimpse-picker`.",
            "type": "object",
            "properties": {
              "extends": {
                "type": "string",
                "const": "color-picker"
              },
              "settings-command": {
                "description": "The program that row runs, as a command and its arguments:\n`[\"xdg-open\", \"https://calendar.google.com/\"]`. It is a list rather than one string because\nthere is no shell between here and the program — an argument containing a space is one\nelement, and nothing has to be quoted, escaped or protected from word splitting.",
                "type": "array",
                "items": {
                  "type": "string"
                }
              },
              "settings-label": {
                "description": "The label on the row the applet's popover puts in its footer. Set it together with\n`settings-command`.",
                "type": [
                  "string",
                  "null"
                ]
              },
              "tooltip-format": {
                "description": "What the bar's tooltip reads. The tokens are the applet's own — `strftime` for the clock,\n`{index}` and `{name}` for the pager, `{summary}`, `{detail}`, `{when}` and `{conflicts}`\nfor next-event — the same way `label` already differs between them. A token with nothing\nbehind it renders as nothing. Unset means the applet shows no tooltip.",
                "type": [
                  "string",
                  "null"
                ]
              }
            },
            "additionalProperties": false,
            "required": [
              "extends"
            ]
          },
          {
            "description": "A chip the user defines, running a program on a click or a scroll.",
            "type": "object",
            "properties": {
              "extends": {
                "type": "string",
                "const": "command"
              },
              "icon": {
                "description": "A themed icon name, or the absolute path to an image. Prefer a `-symbolic` name, which\nfollows the bar's color.",
                "type": [
                  "string",
                  "null"
                ]
              },
              "label": {
                "description": "Text beside the icon. With neither an icon nor a label the chip takes no room.",
                "type": [
                  "string",
                  "null"
                ]
              },
              "on-click": {
                "description": "What a left click runs.",
                "type": "array",
                "items": {
                  "type": "string"
                }
              },
              "on-middle-click": {
                "description": "What a middle click runs.",
                "type": "array",
                "items": {
                  "type": "string"
                }
              },
              "on-right-click": {
                "description": "What a right click runs.",
                "type": "array",
                "items": {
                  "type": "string"
                }
              },
              "on-scroll-down": {
                "description": "What each notch of scrolling down runs, once per notch.",
                "type": "array",
                "items": {
                  "type": "string"
                }
              },
              "on-scroll-left": {
                "description": "What each notch of scrolling left runs, once per notch.",
                "type": "array",
                "items": {
                  "type": "string"
                }
              },
              "on-scroll-right": {
                "description": "What each notch of scrolling right runs, once per notch.",
                "type": "array",
                "items": {
                  "type": "string"
                }
              },
              "on-scroll-up": {
                "description": "What each notch of scrolling up runs, once per notch.",
                "type": "array",
                "items": {
                  "type": "string"
                }
              },
              "settings-command": {
                "description": "The program that row runs, as a command and its arguments:\n`[\"xdg-open\", \"https://calendar.google.com/\"]`. It is a list rather than one string because\nthere is no shell between here and the program — an argument containing a space is one\nelement, and nothing has to be quoted, escaped or protected from word splitting.",
                "type": "array",
                "items": {
                  "type": "string"
                }
              },
              "settings-label": {
                "description": "The label on the row the applet's popover puts in its footer. Set it together with\n`settings-command`.",
                "type": [
                  "string",
                  "null"
                ]
              },
              "tooltip-format": {
                "description": "What the bar's tooltip reads. The tokens are the applet's own — `strftime` for the clock,\n`{index}` and `{name}` for the pager, `{summary}`, `{detail}`, `{when}` and `{conflicts}`\nfor next-event — the same way `label` already differs between them. A token with nothing\nbehind it renders as nothing. Unset means the applet shows no tooltip.",
                "type": [
                  "string",
                  "null"
                ]
              }
            },
            "additionalProperties": false,
            "required": [
              "extends"
            ]
          },
          {
            "description": "Connected outputs, their modes and their arrangement.",
            "type": "object",
            "properties": {
              "extends": {
                "type": "string",
                "const": "display"
              },
              "settings-command": {
                "description": "The program that row runs, as a command and its arguments:\n`[\"xdg-open\", \"https://calendar.google.com/\"]`. It is a list rather than one string because\nthere is no shell between here and the program — an argument containing a space is one\nelement, and nothing has to be quoted, escaped or protected from word splitting.",
                "type": "array",
                "items": {
                  "type": "string"
                }
              },
              "settings-label": {
                "description": "The label on the row the applet's popover puts in its footer. Set it together with\n`settings-command`.",
                "type": [
                  "string",
                  "null"
                ]
              },
              "tooltip-format": {
                "description": "What the bar's tooltip reads. The tokens are the applet's own — `strftime` for the clock,\n`{index}` and `{name}` for the pager, `{summary}`, `{detail}`, `{when}` and `{conflicts}`\nfor next-event — the same way `label` already differs between them. A token with nothing\nbehind it renders as nothing. Unset means the applet shows no tooltip.",
                "type": [
                  "string",
                  "null"
                ]
              }
            },
            "additionalProperties": false,
            "required": [
              "extends"
            ]
          },
          {
            "description": "Hosts a third-party applet binary that draws its own popover.",
            "type": "object",
            "properties": {
              "extends": {
                "type": "string",
                "const": "exec"
              },
              "settings-command": {
                "description": "The program that row runs, as a command and its arguments:\n`[\"xdg-open\", \"https://calendar.google.com/\"]`. It is a list rather than one string because\nthere is no shell between here and the program — an argument containing a space is one\nelement, and nothing has to be quoted, escaped or protected from word splitting.",
                "type": "array",
                "items": {
                  "type": "string"
                }
              },
              "settings-label": {
                "description": "The label on the row the applet's popover puts in its footer. Set it together with\n`settings-command`.",
                "type": [
                  "string",
                  "null"
                ]
              },
              "tooltip-format": {
                "description": "What the bar's tooltip reads. The tokens are the applet's own — `strftime` for the clock,\n`{index}` and `{name}` for the pager, `{summary}`, `{detail}`, `{when}` and `{conflicts}`\nfor next-event — the same way `label` already differs between them. A token with nothing\nbehind it renders as nothing. Unset means the applet shows no tooltip.",
                "type": [
                  "string",
                  "null"
                ]
              }
            },
            "additionalProperties": false,
            "required": [
              "extends"
            ]
          },
          {
            "description": "A counter that ticks once a second. A development fixture: it proves the service is\nreachable and events are arriving, and is not meant for a real bar.",
            "type": "object",
            "properties": {
              "extends": {
                "type": "string",
                "const": "heartbeat"
              },
              "settings-command": {
                "description": "The program that row runs, as a command and its arguments:\n`[\"xdg-open\", \"https://calendar.google.com/\"]`. It is a list rather than one string because\nthere is no shell between here and the program — an argument containing a space is one\nelement, and nothing has to be quoted, escaped or protected from word splitting.",
                "type": "array",
                "items": {
                  "type": "string"
                }
              },
              "settings-label": {
                "description": "The label on the row the applet's popover puts in its footer. Set it together with\n`settings-command`.",
                "type": [
                  "string",
                  "null"
                ]
              },
              "tooltip-format": {
                "description": "What the bar's tooltip reads. The tokens are the applet's own — `strftime` for the clock,\n`{index}` and `{name}` for the pager, `{summary}`, `{detail}`, `{when}` and `{conflicts}`\nfor next-event — the same way `label` already differs between them. A token with nothing\nbehind it renders as nothing. Unset means the applet shows no tooltip.",
                "type": [
                  "string",
                  "null"
                ]
              }
            },
            "additionalProperties": false,
            "required": [
              "extends"
            ]
          },
          {
            "description": "Idle inhibition, for keeping the screen awake.",
            "type": "object",
            "properties": {
              "extends": {
                "type": "string",
                "const": "idle"
              },
              "settings-command": {
                "description": "The program that row runs, as a command and its arguments:\n`[\"xdg-open\", \"https://calendar.google.com/\"]`. It is a list rather than one string because\nthere is no shell between here and the program — an argument containing a space is one\nelement, and nothing has to be quoted, escaped or protected from word splitting.",
                "type": "array",
                "items": {
                  "type": "string"
                }
              },
              "settings-label": {
                "description": "The label on the row the applet's popover puts in its footer. Set it together with\n`settings-command`.",
                "type": [
                  "string",
                  "null"
                ]
              },
              "tooltip-format": {
                "description": "What the bar's tooltip reads. The tokens are the applet's own — `strftime` for the clock,\n`{index}` and `{name}` for the pager, `{summary}`, `{detail}`, `{when}` and `{conflicts}`\nfor next-event — the same way `label` already differs between them. A token with nothing\nbehind it renders as nothing. Unset means the applet shows no tooltip.",
                "type": [
                  "string",
                  "null"
                ]
              }
            },
            "additionalProperties": false,
            "required": [
              "extends"
            ]
          },
          {
            "description": "The active keyboard layout, and switches between the configured ones.",
            "type": "object",
            "properties": {
              "extends": {
                "type": "string",
                "const": "keyboard"
              },
              "settings-command": {
                "description": "The program that row runs, as a command and its arguments:\n`[\"xdg-open\", \"https://calendar.google.com/\"]`. It is a list rather than one string because\nthere is no shell between here and the program — an argument containing a space is one\nelement, and nothing has to be quoted, escaped or protected from word splitting.",
                "type": "array",
                "items": {
                  "type": "string"
                }
              },
              "settings-label": {
                "description": "The label on the row the applet's popover puts in its footer. Set it together with\n`settings-command`.",
                "type": [
                  "string",
                  "null"
                ]
              },
              "tooltip-format": {
                "description": "What the bar's tooltip reads. The tokens are the applet's own — `strftime` for the clock,\n`{index}` and `{name}` for the pager, `{summary}`, `{detail}`, `{when}` and `{conflicts}`\nfor next-event — the same way `label` already differs between them. A token with nothing\nbehind it renders as nothing. Unset means the applet shows no tooltip.",
                "type": [
                  "string",
                  "null"
                ]
              }
            },
            "additionalProperties": false,
            "required": [
              "extends"
            ]
          },
          {
            "description": "The currently playing track, with transport controls in its popover.",
            "type": "object",
            "properties": {
              "extends": {
                "type": "string",
                "const": "mpris"
              },
              "label-format": {
                "description": "What the bar shows. Placeholders are replaced by name: `{player}`, `{title}`, `{artist}`,\n`{album}`, `{state}`, `{position}`, `{duration}` and `{remaining}`. A placeholder with\nnothing behind it renders as nothing.",
                "type": "string",
                "default": "{title} — {artist}"
              },
              "max-length": {
                "description": "Longest label the bar shows, in characters; the rest is ellipsized. Track titles come from\nwhatever is playing and are unbounded, and a bar that resizes as they change is worse than\none that truncates.",
                "type": "integer",
                "format": "uint8",
                "default": 40,
                "maximum": 255,
                "minimum": 0
              },
              "settings-command": {
                "description": "The program that row runs, as a command and its arguments:\n`[\"xdg-open\", \"https://calendar.google.com/\"]`. It is a list rather than one string because\nthere is no shell between here and the program — an argument containing a space is one\nelement, and nothing has to be quoted, escaped or protected from word splitting.",
                "type": "array",
                "items": {
                  "type": "string"
                }
              },
              "settings-label": {
                "description": "The label on the row the applet's popover puts in its footer. Set it together with\n`settings-command`.",
                "type": [
                  "string",
                  "null"
                ]
              },
              "show-art": {
                "description": "Whether the popover shows album art.",
                "type": "boolean",
                "default": true
              },
              "show-others": {
                "description": "Whether the popover lists the players other than the one on the bar.",
                "type": "boolean",
                "default": true
              },
              "tooltip-format": {
                "description": "What the bar's tooltip reads. The tokens are the applet's own — `strftime` for the clock,\n`{index}` and `{name}` for the pager, `{summary}`, `{detail}`, `{when}` and `{conflicts}`\nfor next-event — the same way `label` already differs between them. A token with nothing\nbehind it renders as nothing. Unset means the applet shows no tooltip.",
                "type": [
                  "string",
                  "null"
                ]
              }
            },
            "additionalProperties": false,
            "required": [
              "extends"
            ]
          },
          {
            "description": "Connection state, with the available networks in its popover.",
            "type": "object",
            "properties": {
              "extends": {
                "type": "string",
                "const": "network"
              },
              "metered-in-tooltip": {
                "description": "Whether a metered connection is named in the tooltip. It is always marked on the row inside\nthe popover; this is only about the bar.",
                "type": "boolean",
                "default": true
              },
              "settings-command": {
                "description": "The program that row runs, as a command and its arguments:\n`[\"xdg-open\", \"https://calendar.google.com/\"]`. It is a list rather than one string because\nthere is no shell between here and the program — an argument containing a space is one\nelement, and nothing has to be quoted, escaped or protected from word splitting.",
                "type": "array",
                "items": {
                  "type": "string"
                }
              },
              "settings-label": {
                "description": "The label on the row the applet's popover puts in its footer. Set it together with\n`settings-command`.",
                "type": [
                  "string",
                  "null"
                ]
              },
              "tooltip-format": {
                "description": "What the bar's tooltip reads. The tokens are the applet's own — `strftime` for the clock,\n`{index}` and `{name}` for the pager, `{summary}`, `{detail}`, `{when}` and `{conflicts}`\nfor next-event — the same way `label` already differs between them. A token with nothing\nbehind it renders as nothing. Unset means the applet shows no tooltip.",
                "type": [
                  "string",
                  "null"
                ]
              },
              "visible-networks": {
                "description": "How many networks the popover lists before the rest go behind a \"more networks\" row. `0`\nlists every network in range.",
                "type": "integer",
                "format": "uint",
                "default": 8,
                "minimum": 0
              },
              "vpn-chip": {
                "description": "Whether an active VPN gets a chip of its own beside the network chip. An indicator is an\nicon, and two states overlaid on one glyph are illegible at bar size.",
                "type": "boolean",
                "default": true
              }
            },
            "additionalProperties": false,
            "required": [
              "extends"
            ]
          },
          {
            "description": "The next entry from the configured calendars.",
            "type": "object",
            "properties": {
              "all-day": {
                "description": "Whether an all-day entry may take the bar. A timed event always wins over one, so this\nonly decides what happens on a day holding nothing else — leaving it off keeps a week of\nleave from pinning the applet open for the whole week.",
                "type": "boolean",
                "default": false
              },
              "countdown": {
                "description": "How close an event has to be before the bar spells out how long is left, in minutes. Inside\nit the indicator reads `Design review in 12 min`, or `ends in 25 min` once it has started;\noutside it the title stands alone. Set it below `within` to have an entry appear quietly and\nstart counting only as it approaches; `0` never counts.",
                "type": "integer",
                "format": "uint64",
                "default": 60,
                "minimum": 0
              },
              "extends": {
                "type": "string",
                "const": "next-event"
              },
              "horizon": {
                "description": "How far ahead the popover's list reaches, in minutes. This is the second of two windows and\nthe wider one: `within` decides when the bar lights up, `horizon` decides what the list\nholds once you open it. The default 720 is twelve hours, so a late-afternoon glance still\nreaches tomorrow morning — a plain \"today only\" rule empties the list at exactly the hour\ntomorrow's first meeting starts mattering. 1440 is a rolling day.",
                "type": "integer",
                "format": "uint64",
                "default": 720,
                "minimum": 0
              },
              "settings-command": {
                "description": "The program that row runs, as a command and its arguments:\n`[\"xdg-open\", \"https://calendar.google.com/\"]`. It is a list rather than one string because\nthere is no shell between here and the program — an argument containing a space is one\nelement, and nothing has to be quoted, escaped or protected from word splitting.",
                "type": "array",
                "items": {
                  "type": "string"
                }
              },
              "settings-label": {
                "description": "The label on the row the applet's popover puts in its footer. Set it together with\n`settings-command`.",
                "type": [
                  "string",
                  "null"
                ]
              },
              "tooltip-format": {
                "description": "What the bar's tooltip reads. The tokens are the applet's own — `strftime` for the clock,\n`{index}` and `{name}` for the pager, `{summary}`, `{detail}`, `{when}` and `{conflicts}`\nfor next-event — the same way `label` already differs between them. A token with nothing\nbehind it renders as nothing. Unset means the applet shows no tooltip.",
                "type": [
                  "string",
                  "null"
                ]
              },
              "upcoming": {
                "description": "How many entries the popover lists under the one it is showing, whichever `horizon` leaves.\nCapped at 20, because a list longer than that is a popover taller than the screen.",
                "type": "integer",
                "format": "uint",
                "default": 5,
                "minimum": 0
              },
              "within": {
                "description": "How long before an event starts it takes the bar, in minutes. Anything further out than\nthis leaves the applet empty, which is what keeps a meeting two days away off a bar that\nhas nothing to say about it. An event that has already started stays until it ends,\nhowever long it has been running, so `0` shows only what is under way.",
                "type": "integer",
                "format": "uint64",
                "default": 60,
                "minimum": 0
              }
            },
            "additionalProperties": false,
            "required": [
              "extends"
            ]
          },
          {
            "description": "Unread notifications, with their history in its popover.",
            "type": "object",
            "properties": {
              "extends": {
                "type": "string",
                "const": "notifications"
              },
              "indicator-style": {
                "$ref": "#/$defs/NotificationIndicatorStyle",
                "default": "icon-dot"
              },
              "settings-command": {
                "description": "The program that row runs, as a command and its arguments:\n`[\"xdg-open\", \"https://calendar.google.com/\"]`. It is a list rather than one string because\nthere is no shell between here and the program — an argument containing a space is one\nelement, and nothing has to be quoted, escaped or protected from word splitting.",
                "type": "array",
                "items": {
                  "type": "string"
                }
              },
              "settings-label": {
                "description": "The label on the row the applet's popover puts in its footer. Set it together with\n`settings-command`.",
                "type": [
                  "string",
                  "null"
                ]
              },
              "tooltip-format": {
                "description": "What the bar's tooltip reads. The tokens are the applet's own — `strftime` for the clock,\n`{index}` and `{name}` for the pager, `{summary}`, `{detail}`, `{when}` and `{conflicts}`\nfor next-event — the same way `label` already differs between them. A token with nothing\nbehind it renders as nothing. Unset means the applet shows no tooltip.",
                "type": [
                  "string",
                  "null"
                ]
              }
            },
            "additionalProperties": false,
            "required": [
              "extends"
            ]
          },
          {
            "description": "A strip of workspaces or windows, one slot each, that switches between them on a click.",
            "type": "object",
            "properties": {
              "extends": {
                "type": "string",
                "const": "pager"
              },
              "focused-label": {
                "description": "The label for the slot the user is on, so the current workspace can show its name while\nthe rest stay numbers. Same tokens as `label`; unset falls back to it.",
                "type": [
                  "string",
                  "null"
                ]
              },
              "label": {
                "description": "What a slot reads, when the shape is `labels`, and the fallback for every state below.\nUnderstands `{index}`, `{id}`, `{name}`, `{name-or-index}` and `{workspace-name}`.\n`{index}` falls back to the id, because only niri numbers its workspaces separately from\ntheir ids; in `windows` mode it is the slot's position and `{name}` is the window's app\nid, which is why `{workspace-name}` exists.",
                "type": "string",
                "default": "{index}"
              },
              "mode": {
                "description": "Whether each slot is a workspace or a window.",
                "$ref": "#/$defs/PagerMode",
                "default": "workspaces"
              },
              "scope": {
                "description": "How much of the session the strip covers.",
                "$ref": "#/$defs/PagerScope",
                "default": "output"
              },
              "settings-command": {
                "description": "The program that row runs, as a command and its arguments:\n`[\"xdg-open\", \"https://calendar.google.com/\"]`. It is a list rather than one string because\nthere is no shell between here and the program — an argument containing a space is one\nelement, and nothing has to be quoted, escaped or protected from word splitting.",
                "type": "array",
                "items": {
                  "type": "string"
                }
              },
              "settings-label": {
                "description": "The label on the row the applet's popover puts in its footer. Set it together with\n`settings-command`.",
                "type": [
                  "string",
                  "null"
                ]
              },
              "shape": {
                "description": "Whether a slot is drawn as a dot or as its label in a pill.",
                "$ref": "#/$defs/PagerShape",
                "default": "dots"
              },
              "tooltip-format": {
                "description": "What the bar's tooltip reads. The tokens are the applet's own — `strftime` for the clock,\n`{index}` and `{name}` for the pager, `{summary}`, `{detail}`, `{when}` and `{conflicts}`\nfor next-event — the same way `label` already differs between them. A token with nothing\nbehind it renders as nothing. Unset means the applet shows no tooltip.",
                "type": [
                  "string",
                  "null"
                ]
              },
              "unfocused-label": {
                "description": "The label for every slot the user is not on. Same tokens as `label`; unset falls back\nto it.",
                "type": [
                  "string",
                  "null"
                ]
              },
              "urgent-label": {
                "description": "The label for a slot asking for attention. Takes precedence over the other two, because a\nwindow wanting attention is the one thing the strip exists to surface. Same tokens as\n`label`; unset falls back to it.",
                "type": [
                  "string",
                  "null"
                ]
              }
            },
            "additionalProperties": false,
            "required": [
              "extends"
            ]
          },
          {
            "description": "Home and user directories, bookmarks, network shares and the trash.",
            "type": "object",
            "properties": {
              "bookmarks": {
                "description": "How many bookmarks the popover lists before the rest go behind a drawer.",
                "type": "integer",
                "format": "uint",
                "default": 8,
                "minimum": 0
              },
              "extends": {
                "type": "string",
                "const": "places"
              },
              "settings-command": {
                "description": "The program that row runs, as a command and its arguments:\n`[\"xdg-open\", \"https://calendar.google.com/\"]`. It is a list rather than one string because\nthere is no shell between here and the program — an argument containing a space is one\nelement, and nothing has to be quoted, escaped or protected from word splitting.",
                "type": "array",
                "items": {
                  "type": "string"
                }
              },
              "settings-label": {
                "description": "The label on the row the applet's popover puts in its footer. Set it together with\n`settings-command`.",
                "type": [
                  "string",
                  "null"
                ]
              },
              "tooltip-format": {
                "description": "What the bar's tooltip reads. The tokens are the applet's own — `strftime` for the clock,\n`{index}` and `{name}` for the pager, `{summary}`, `{detail}`, `{when}` and `{conflicts}`\nfor next-event — the same way `label` already differs between them. A token with nothing\nbehind it renders as nothing. Unset means the applet shows no tooltip.",
                "type": [
                  "string",
                  "null"
                ]
              }
            },
            "additionalProperties": false,
            "required": [
              "extends"
            ]
          },
          {
            "description": "Active print jobs.",
            "type": "object",
            "properties": {
              "extends": {
                "type": "string",
                "const": "printing"
              },
              "jobs": {
                "description": "Print jobs listed in the popover before older ones are hidden.",
                "type": "integer",
                "format": "uint",
                "default": 8,
                "minimum": 0
              },
              "settings-command": {
                "description": "The program that row runs, as a command and its arguments:\n`[\"xdg-open\", \"https://calendar.google.com/\"]`. It is a list rather than one string because\nthere is no shell between here and the program — an argument containing a space is one\nelement, and nothing has to be quoted, escaped or protected from word splitting.",
                "type": "array",
                "items": {
                  "type": "string"
                }
              },
              "settings-label": {
                "description": "The label on the row the applet's popover puts in its footer. Set it together with\n`settings-command`.",
                "type": [
                  "string",
                  "null"
                ]
              },
              "tooltip-format": {
                "description": "What the bar's tooltip reads. The tokens are the applet's own — `strftime` for the clock,\n`{index}` and `{name}` for the pager, `{summary}`, `{detail}`, `{when}` and `{conflicts}`\nfor next-event — the same way `label` already differs between them. A token with nothing\nbehind it renders as nothing. Unset means the applet shows no tooltip.",
                "type": [
                  "string",
                  "null"
                ]
              }
            },
            "additionalProperties": false,
            "required": [
              "extends"
            ]
          },
          {
            "description": "Shows when the microphone, camera or screen is in use.",
            "type": "object",
            "properties": {
              "extends": {
                "type": "string",
                "const": "privacy"
              },
              "settings-command": {
                "description": "The program that row runs, as a command and its arguments:\n`[\"xdg-open\", \"https://calendar.google.com/\"]`. It is a list rather than one string because\nthere is no shell between here and the program — an argument containing a space is one\nelement, and nothing has to be quoted, escaped or protected from word splitting.",
                "type": "array",
                "items": {
                  "type": "string"
                }
              },
              "settings-label": {
                "description": "The label on the row the applet's popover puts in its footer. Set it together with\n`settings-command`.",
                "type": [
                  "string",
                  "null"
                ]
              },
              "show-camera": {
                "description": "Whether the camera is shown when in use.",
                "type": "boolean",
                "default": true
              },
              "show-location": {
                "description": "Whether location access is shown when in use.",
                "type": "boolean",
                "default": true
              },
              "show-microphone": {
                "description": "Whether the microphone is shown when in use.",
                "type": "boolean",
                "default": true
              },
              "show-screencast": {
                "description": "Whether screen capture is shown when in use.",
                "type": "boolean",
                "default": true
              },
              "tooltip-format": {
                "description": "What the bar's tooltip reads. The tokens are the applet's own — `strftime` for the clock,\n`{index}` and `{name}` for the pager, `{summary}`, `{detail}`, `{when}` and `{conflicts}`\nfor next-event — the same way `label` already differs between them. A token with nothing\nbehind it renders as nothing. Unset means the applet shows no tooltip.",
                "type": [
                  "string",
                  "null"
                ]
              }
            },
            "additionalProperties": false,
            "required": [
              "extends"
            ]
          },
          {
            "description": "Removable drives and their volumes, with mount, unmount and eject in its popover.",
            "type": "object",
            "properties": {
              "extends": {
                "type": "string",
                "const": "removable"
              },
              "settings-command": {
                "description": "The program that row runs, as a command and its arguments:\n`[\"xdg-open\", \"https://calendar.google.com/\"]`. It is a list rather than one string because\nthere is no shell between here and the program — an argument containing a space is one\nelement, and nothing has to be quoted, escaped or protected from word splitting.",
                "type": "array",
                "items": {
                  "type": "string"
                }
              },
              "settings-label": {
                "description": "The label on the row the applet's popover puts in its footer. Set it together with\n`settings-command`.",
                "type": [
                  "string",
                  "null"
                ]
              },
              "tooltip-format": {
                "description": "What the bar's tooltip reads. The tokens are the applet's own — `strftime` for the clock,\n`{index}` and `{name}` for the pager, `{summary}`, `{detail}`, `{when}` and `{conflicts}`\nfor next-event — the same way `label` already differs between them. A token with nothing\nbehind it renders as nothing. Unset means the applet shows no tooltip.",
                "type": [
                  "string",
                  "null"
                ]
              },
              "volumes": {
                "description": "How many drives the popover lists before the rest go behind a drawer.",
                "type": "integer",
                "format": "uint",
                "default": 6,
                "minimum": 0
              }
            },
            "additionalProperties": false,
            "required": [
              "extends"
            ]
          },
          {
            "description": "Measures on-screen pixel distances. A left click opens the lens to measure from the\nscreen. Needs `glimpse-ruler`.",
            "type": "object",
            "properties": {
              "extends": {
                "type": "string",
                "const": "ruler"
              },
              "settings-command": {
                "description": "The program that row runs, as a command and its arguments:\n`[\"xdg-open\", \"https://calendar.google.com/\"]`. It is a list rather than one string because\nthere is no shell between here and the program — an argument containing a space is one\nelement, and nothing has to be quoted, escaped or protected from word splitting.",
                "type": "array",
                "items": {
                  "type": "string"
                }
              },
              "settings-label": {
                "description": "The label on the row the applet's popover puts in its footer. Set it together with\n`settings-command`.",
                "type": [
                  "string",
                  "null"
                ]
              },
              "tooltip-format": {
                "description": "What the bar's tooltip reads. The tokens are the applet's own — `strftime` for the clock,\n`{index}` and `{name}` for the pager, `{summary}`, `{detail}`, `{when}` and `{conflicts}`\nfor next-event — the same way `label` already differs between them. A token with nothing\nbehind it renders as nothing. Unset means the applet shows no tooltip.",
                "type": [
                  "string",
                  "null"
                ]
              }
            },
            "additionalProperties": false,
            "required": [
              "extends"
            ]
          },
          {
            "description": "Log out, suspend, restart and shut down.",
            "type": "object",
            "properties": {
              "extends": {
                "type": "string",
                "const": "session"
              },
              "settings-command": {
                "description": "The program that row runs, as a command and its arguments:\n`[\"xdg-open\", \"https://calendar.google.com/\"]`. It is a list rather than one string because\nthere is no shell between here and the program — an argument containing a space is one\nelement, and nothing has to be quoted, escaped or protected from word splitting.",
                "type": "array",
                "items": {
                  "type": "string"
                }
              },
              "settings-label": {
                "description": "The label on the row the applet's popover puts in its footer. Set it together with\n`settings-command`.",
                "type": [
                  "string",
                  "null"
                ]
              },
              "tooltip-format": {
                "description": "What the bar's tooltip reads. The tokens are the applet's own — `strftime` for the clock,\n`{index}` and `{name}` for the pager, `{summary}`, `{detail}`, `{when}` and `{conflicts}`\nfor next-event — the same way `label` already differs between them. A token with nothing\nbehind it renders as nothing. Unset means the applet shows no tooltip.",
                "type": [
                  "string",
                  "null"
                ]
              }
            },
            "additionalProperties": false,
            "required": [
              "extends"
            ]
          },
          {
            "description": "Live CPU, RAM, swap, disk, network and (amdgpu) GPU load. Its backing service samples\nnothing at all unless this kind is actually placed on a panel — see `placed_kinds`.",
            "type": "object",
            "properties": {
              "chip-format": {
                "description": "How each chip's own label reads. `{name}` is the chip's own localized name (\"CPU\", \"RAM\",\n…); `{value}` is its reading (\"42%\" for a percentage chip, \"↓1.2 MB/s\" for network). An\nunrecognized token is left as literal text rather than silently emptied.",
                "type": "string",
                "default": "{name} {value}"
              },
              "chips": {
                "description": "Chips shown on the bar, in order. Anything sampled but left out here still appears in the\npopover — this list is panel real estate, not visibility. A repeated chip collapses to its\nfirst occurrence.",
                "type": "array",
                "default": [
                  "cpu",
                  "ram"
                ],
                "items": {
                  "$ref": "#/$defs/Chip"
                }
              },
              "critical-percent": {
                "description": "Percent at and above which a reading turns `Severity::Error`. This codebase's `Severity`\nenum has no \"Critical\" variant — it is `Info`/`Warning`/`Error` only.",
                "type": "integer",
                "format": "uint8",
                "default": 95,
                "maximum": 100,
                "minimum": 1
              },
              "extends": {
                "type": "string",
                "const": "system-monitor"
              },
              "settings-command": {
                "description": "The program that row runs, as a command and its arguments:\n`[\"xdg-open\", \"https://calendar.google.com/\"]`. It is a list rather than one string because\nthere is no shell between here and the program — an argument containing a space is one\nelement, and nothing has to be quoted, escaped or protected from word splitting.",
                "type": "array",
                "items": {
                  "type": "string"
                }
              },
              "settings-label": {
                "description": "The label on the row the applet's popover puts in its footer. Set it together with\n`settings-command`.",
                "type": [
                  "string",
                  "null"
                ]
              },
              "tooltip-format": {
                "description": "What the bar's tooltip reads. The tokens are the applet's own — `strftime` for the clock,\n`{index}` and `{name}` for the pager, `{summary}`, `{detail}`, `{when}` and `{conflicts}`\nfor next-event — the same way `label` already differs between them. A token with nothing\nbehind it renders as nothing. Unset means the applet shows no tooltip.",
                "type": [
                  "string",
                  "null"
                ]
              },
              "warn-percent": {
                "description": "Percent at and above which any percentage reading (CPU, RAM, swap, GPU usage/memory) turns\n`Severity::Warning`. Network has no percentage and is never colored.",
                "type": "integer",
                "format": "uint8",
                "default": 85,
                "maximum": 100,
                "minimum": 1
              }
            },
            "additionalProperties": false,
            "required": [
              "extends"
            ]
          },
          {
            "description": "The system tray: icons from applications that ask for one.",
            "type": "object",
            "properties": {
              "extends": {
                "type": "string",
                "const": "tray"
              },
              "hide": {
                "description": "Item ids never shown, matched against the application's own `Id`. That id survives an\napplication restart, which the bus name it happens to hold does not.",
                "type": "array",
                "default": [],
                "items": {
                  "type": "string"
                }
              },
              "max-visible": {
                "description": "How many icons stay on the bar; the rest open from the chevron beside them. `0` keeps every\nicon on the bar and shows no chevron.",
                "type": "integer",
                "format": "uint8",
                "default": 0,
                "maximum": 255,
                "minimum": 0
              },
              "pin": {
                "description": "Item ids kept on the bar whatever the cap, in the order given. Anything not named here\nfollows in the order the items registered.",
                "type": "array",
                "default": [],
                "items": {
                  "type": "string"
                }
              },
              "settings-command": {
                "description": "The program that row runs, as a command and its arguments:\n`[\"xdg-open\", \"https://calendar.google.com/\"]`. It is a list rather than one string because\nthere is no shell between here and the program — an argument containing a space is one\nelement, and nothing has to be quoted, escaped or protected from word splitting.",
                "type": "array",
                "items": {
                  "type": "string"
                }
              },
              "settings-label": {
                "description": "The label on the row the applet's popover puts in its footer. Set it together with\n`settings-command`.",
                "type": [
                  "string",
                  "null"
                ]
              },
              "tooltip-format": {
                "description": "What the bar's tooltip reads. The tokens are the applet's own — `strftime` for the clock,\n`{index}` and `{name}` for the pager, `{summary}`, `{detail}`, `{when}` and `{conflicts}`\nfor next-event — the same way `label` already differs between them. A token with nothing\nbehind it renders as nothing. Unset means the applet shows no tooltip.",
                "type": [
                  "string",
                  "null"
                ]
              }
            },
            "additionalProperties": false,
            "required": [
              "extends"
            ]
          },
          {
            "description": "Current conditions, with the forecast in its popover.",
            "type": "object",
            "properties": {
              "days": {
                "description": "How many days the popover's list shows. The list starts at tomorrow — today is already\nthe hero, the strip and the details page, and repeating it as a row says nothing new.",
                "type": "integer",
                "format": "uint8",
                "default": 6,
                "maximum": 255,
                "minimum": 0
              },
              "extends": {
                "type": "string",
                "const": "weather"
              },
              "hours": {
                "description": "How many hours the popover's strip shows. The strip starts at the next hour — the hour\nstanding is already the hero.",
                "type": "integer",
                "format": "uint8",
                "default": 4,
                "maximum": 255,
                "minimum": 0
              },
              "label": {
                "description": "What the bar and the popover call this place. Unset means a fixed place reads as the pair\nit was given, and `here` as whatever the geolocation service resolved.",
                "type": [
                  "string",
                  "null"
                ]
              },
              "place": {
                "description": "Which place this applet shows.",
                "$ref": "#/$defs/Place",
                "default": {
                  "at": "here"
                }
              },
              "settings-command": {
                "description": "The program that row runs, as a command and its arguments:\n`[\"xdg-open\", \"https://calendar.google.com/\"]`. It is a list rather than one string because\nthere is no shell between here and the program — an argument containing a space is one\nelement, and nothing has to be quoted, escaped or protected from word splitting.",
                "type": "array",
                "items": {
                  "type": "string"
                }
              },
              "settings-label": {
                "description": "The label on the row the applet's popover puts in its footer. Set it together with\n`settings-command`.",
                "type": [
                  "string",
                  "null"
                ]
              },
              "tooltip-format": {
                "description": "What the bar's tooltip reads. The tokens are the applet's own — `strftime` for the clock,\n`{index}` and `{name}` for the pager, `{summary}`, `{detail}`, `{when}` and `{conflicts}`\nfor next-event — the same way `label` already differs between them. A token with nothing\nbehind it renders as nothing. Unset means the applet shows no tooltip.",
                "type": [
                  "string",
                  "null"
                ]
              }
            },
            "additionalProperties": false,
            "required": [
              "extends"
            ]
          },
          {
            "description": "The current workspace's name, renamed from its popover.",
            "type": "object",
            "properties": {
              "extends": {
                "type": "string",
                "const": "workspace-name"
              },
              "settings-command": {
                "description": "The program that row runs, as a command and its arguments:\n`[\"xdg-open\", \"https://calendar.google.com/\"]`. It is a list rather than one string because\nthere is no shell between here and the program — an argument containing a space is one\nelement, and nothing has to be quoted, escaped or protected from word splitting.",
                "type": "array",
                "items": {
                  "type": "string"
                }
              },
              "settings-label": {
                "description": "The label on the row the applet's popover puts in its footer. Set it together with\n`settings-command`.",
                "type": [
                  "string",
                  "null"
                ]
              },
              "tooltip-format": {
                "description": "What the bar's tooltip reads. The tokens are the applet's own — `strftime` for the clock,\n`{index}` and `{name}` for the pager, `{summary}`, `{detail}`, `{when}` and `{conflicts}`\nfor next-event — the same way `label` already differs between them. A token with nothing\nbehind it renders as nothing. Unset means the applet shows no tooltip.",
                "type": [
                  "string",
                  "null"
                ]
              }
            },
            "additionalProperties": false,
            "required": [
              "extends"
            ]
          }
        ]
      },
      "default": {}
    },
    "bluetooth": {
      "$ref": "#/$defs/Bluetooth",
      "default": {
        "hide-unnamed": true,
        "scan-timeout": 30
      }
    },
    "brightness": {
      "$ref": "#/$defs/Brightness",
      "default": {
        "ddc": true,
        "minimum": 3
      }
    },
    "calendar": {
      "$ref": "#/$defs/Calendar",
      "default": {
        "poll-interval": 600,
        "sources": []
      }
    },
    "clipboard": {
      "$ref": "#/$defs/Clipboard",
      "default": {
        "capture-images": true,
        "enabled": true,
        "limit": 100,
        "max-entry-kib": 2048,
        "max-total-kib": 10240
      }
    },
    "color-picker": {
      "$ref": "#/$defs/ColorPicker",
      "default": {
        "format": "hex",
        "lens-radius": 106,
        "limit": 8,
        "max-zoom": 30
      }
    },
    "geolocation": {
      "$ref": "#/$defs/Geolocation",
      "default": {
        "provider": "geoclue"
      }
    },
    "idle": {
      "$ref": "#/$defs/Idle",
      "default": {
        "enabled": true,
        "profiles": {
          "ac": {
            "listeners": [
              {
                "on-idle": "loginctl lock-session",
                "on-resume": "",
                "timeout": 900
              },
              {
                "on-idle": "systemctl suspend",
                "on-resume": "",
                "timeout": 3600
              }
            ]
          },
          "battery": {
            "listeners": [
              {
                "on-idle": "loginctl lock-session",
                "on-resume": "",
                "timeout": 900
              },
              {
                "on-idle": "systemctl suspend",
                "on-resume": "",
                "timeout": 1800
              }
            ]
          }
        },
        "respect-inhibitors": true
      }
    },
    "keyboard": {
      "$ref": "#/$defs/Keyboard",
      "default": {
        "labels": {},
        "remember": "window"
      }
    },
    "lock": {
      "$ref": "#/$defs/Lock",
      "default": {
        "background": {
          "blur-radius": 0,
          "color": "#000000",
          "dim": 0.35,
          "dim-dark": 0.5,
          "fit": "cover"
        },
        "clock": {
          "date-format": "%A, {day} %B",
          "enabled": true,
          "time-format": "%H:%M"
        },
        "media": {
          "enabled": true
        },
        "notifications": {
          "enabled": true,
          "privacy": "apps"
        },
        "pam-service": "glimpse-lock",
        "prompt-output": "focused",
        "session": {
          "actions": [
            "suspend",
            "reboot",
            "power-off"
          ],
          "enabled": true
        },
        "status": {
          "enabled": true
        }
      }
    },
    "monitors": {
      "$ref": "#/$defs/Monitors",
      "default": {}
    },
    "mpris": {
      "$ref": "#/$defs/Mpris",
      "default": {
        "art-max-kib": 4096,
        "fetch-art": true,
        "ignore": []
      }
    },
    "network": {
      "$ref": "#/$defs/Network",
      "default": {
        "hide-unnamed": true,
        "scan-timeout": 30,
        "show-vpn": true
      }
    },
    "night-light": {
      "$ref": "#/$defs/NightLight",
      "default": {
        "schedule": "automatic",
        "temperature": 4200,
        "transition-minutes": 15
      }
    },
    "notifications": {
      "$ref": "#/$defs/Notifications",
      "default": {
        "edge": "top-right",
        "enabled": true,
        "hide-delay": 4,
        "keep": 100,
        "max-items": 6,
        "monitor": null,
        "suppress": []
      }
    },
    "panels": {
      "type": "array",
      "default": [
        {
          "center": [
            "clock",
            "weather",
            "notifications",
            "privacy"
          ],
          "left": [
            "workspace-name",
            "pager",
            "places",
            "mpris"
          ],
          "margin": {
            "bottom": 0,
            "left": 0,
            "right": 0,
            "top": 0
          },
          "position": "top",
          "right": [
            "next-event",
            "tray",
            "removable",
            "clipboard",
            "color-picker",
            "printing",
            "bluetooth",
            "network",
            "display",
            "brightness",
            "audio",
            "idle",
            "battery",
            "keyboard",
            "session"
          ],
          "size": 36
        }
      ],
      "items": {
        "$ref": "#/$defs/Panel"
      }
    },
    "places": {
      "$ref": "#/$defs/Places",
      "default": {
        "network": true,
        "trash": true
      }
    },
    "power": {
      "$ref": "#/$defs/Power",
      "default": {
        "lock-before-sleep": true,
        "lock-on-request": true
      }
    },
    "printing": {
      "$ref": "#/$defs/Printing",
      "default": {
        "poll-active": 2,
        "poll-idle": 30
      }
    },
    "regional": {
      "$ref": "#/$defs/Regional",
      "default": {
        "hour-format": "locale",
        "language": "",
        "units": "locale"
      }
    },
    "removable": {
      "$ref": "#/$defs/Removable",
      "default": {
        "capacity-interval": 10
      }
    },
    "ruler": {
      "$ref": "#/$defs/Ruler",
      "default": {
        "history-limit": 8,
        "lens-radius": 106,
        "max-zoom": 30
      }
    },
    "system-monitor": {
      "$ref": "#/$defs/SystemMonitor",
      "default": {
        "disk-paths": [
          "/"
        ],
        "gpu": true,
        "poll-interval": 2
      }
    },
    "wallpaper": {
      "$ref": "#/$defs/Wallpaper",
      "default": {
        "backdrop": {
          "blur-radius": 24,
          "downscale-factor": 4,
          "enabled": true
        },
        "color": "#000000",
        "fit": "cover",
        "image": "/usr/share/glimpse/themes/adwaita/default.jpg",
        "outputs": [],
        "transition": "fade",
        "transition-ms": 500
      }
    },
    "weather": {
      "$ref": "#/$defs/Weather",
      "default": {
        "forecast-days": 7,
        "poll-interval": 900,
        "provider": "open-meteo"
      }
    }
  },
  "additionalProperties": false,
  "$defs": {
    "Appearance": {
      "type": "object",
      "properties": {
        "animation-speed": {
          "description": "How fast every animation runs: `1.0` is normal, `2.0` twice as fast, `0.5` half as fast,\nand `0` turns animations off. Anything else below `0.1` is refused, because a fade that\nlong keeps the full-screen popover catcher swallowing clicks. The popover and notification fades and every CSS transition\nthat reads `--gl-duration` follow it; the wallpaper keeps its own `transition-ms`.",
          "type": "number",
          "format": "double",
          "default": 1.0,
          "maximum": 10.0,
          "minimum": 0.0
        },
        "blur": {
          "description": "Surfaces that blur whatever lies behind them, where the compositor offers\next-background-effect-v1; elsewhere they stay opaque. The strength of the blur is the\ncompositor's setting, not glimpse's. niri blurs a layer surface in xray mode unless told\notherwise, which shows the wallpaper rather than the windows under a popover or a\nnotification; `layer-rule { match namespace=\"^glimpse-(popover|notifications)$\";\nbackground-effect { xray false; } }` in the niri config changes that.",
          "type": "array",
          "default": [],
          "items": {
            "$ref": "#/$defs/BlurSurface"
          }
        },
        "color-scheme": {
          "$ref": "#/$defs/ColorScheme",
          "default": "auto"
        },
        "theme": {
          "type": "string",
          "default": "adwaita"
        },
        "theme-variant": {
          "description": "A CSS class added to every window, so one theme can ship several looks. Letters, digits,\n`-` and `_` only, not starting with a digit; anything else is ignored with a warning.",
          "type": "string",
          "default": ""
        }
      },
      "additionalProperties": false
    },
    "Backdrop": {
      "description": "A second background surface, named `glimpse-backdrop`, shown only where the compositor has\nsomewhere to place it — niri's Overview, through a `place-within-backdrop` layer rule.",
      "type": "object",
      "properties": {
        "blur-radius": {
          "description": "Gaussian blur radius in output pixels; 0 blurs nothing.",
          "type": "integer",
          "format": "uint32",
          "default": 24,
          "maximum": 128,
          "minimum": 0
        },
        "downscale-factor": {
          "description": "Divides the output's physical size to get the size the backdrop is decoded at; 1 decodes\nat full size. The shorter side never drops below 256 pixels.",
          "type": "integer",
          "format": "uint32",
          "default": 4,
          "maximum": 16,
          "minimum": 1
        },
        "enabled": {
          "type": "boolean",
          "default": true
        },
        "image": {
          "description": "Source image. Unset derives it from `[wallpaper] image`.",
          "type": [
            "string",
            "null"
          ]
        },
        "image-dark": {
          "description": "Source image under the dark color scheme. Unset derives it from `[wallpaper] image-dark`.",
          "type": [
            "string",
            "null"
          ]
        }
      },
      "additionalProperties": false
    },
    "BackdropOutput": {
      "description": "A per-output override of `[wallpaper.backdrop]`. Every field is optional and falls back to\nthe corresponding value in `[wallpaper.backdrop]` when unset.",
      "type": "object",
      "properties": {
        "blur-radius": {
          "description": "Overrides `[wallpaper.backdrop] blur-radius` for this connector.",
          "type": [
            "integer",
            "null"
          ],
          "format": "uint32",
          "maximum": 128,
          "minimum": 0
        },
        "enabled": {
          "description": "Overrides `[wallpaper.backdrop] enabled` for this connector.",
          "type": [
            "boolean",
            "null"
          ]
        },
        "image": {
          "description": "Overrides `[wallpaper.backdrop] image` for this connector.",
          "type": [
            "string",
            "null"
          ]
        },
        "image-dark": {
          "description": "Overrides `[wallpaper.backdrop] image-dark` for this connector.",
          "type": [
            "string",
            "null"
          ]
        }
      },
      "additionalProperties": false
    },
    "BatteryIndicatorStyle": {
      "type": "string",
      "enum": [
        "icon-only",
        "icon-text",
        "text"
      ]
    },
    "Bluetooth": {
      "type": "object",
      "properties": {
        "hide-unnamed": {
          "description": "Whether to hide nearby devices that advertise no name. Most of them are beacons whose only\nidentity is their own address, and they arrive in large numbers during a scan.",
          "type": "boolean",
          "default": true
        },
        "scan-timeout": {
          "description": "How many seconds a scan for nearby devices runs before stopping on its own. Scanning costs\nbattery and interferes with connected audio, so it is never left running. `0` disables the\ntimeout and leaves the scan running until the popover closes.",
          "type": "integer",
          "format": "uint64",
          "default": 30,
          "minimum": 0
        }
      },
      "additionalProperties": false
    },
    "BlurSurface": {
      "type": "string",
      "enum": [
        "panel",
        "popover",
        "notification"
      ]
    },
    "Brightness": {
      "type": "object",
      "properties": {
        "ddc": {
          "description": "Whether to probe external displays for DDC/CI brightness control over `/dev/i2c-*`. DDC/CI\nis protocol-mandated slow (tens of milliseconds per read or write) and not every monitor\nimplements it correctly, so this stays a document-level escape hatch rather than something\na user has to diagnose by unplugging a display.",
          "type": "boolean",
          "default": true
        },
        "minimum": {
          "description": "The lowest percent a display's backlight is allowed to reach; the keyboard backlight ignores\nit and may still go to 0. Every write glimpse makes to a display source is clamped here,\nbecause a screen driven all the way dark is indistinguishable from one that has failed.",
          "type": "integer",
          "format": "uint8",
          "default": 3,
          "maximum": 100,
          "minimum": 0
        }
      },
      "additionalProperties": false
    },
    "Calendar": {
      "description": "Where calendar events come from. One list, read by every surface that shows an event: the\nclock popover, the day markers on its grid, and the `next-event` applet.",
      "type": "object",
      "properties": {
        "poll-interval": {
          "description": "How often a source that lives on the network is fetched again, in seconds, unless it\nnames its own. Anything local is watched instead and ignores this: a `directory`, and an\n`ical` whose `uri` is a path or a `file://`. The exception is a `file://` holding a\none-line feed URL, which is both — watched for the URL changing, fetched on this interval\nfor the calendar behind it.",
          "type": "integer",
          "format": "uint64",
          "default": 600,
          "minimum": 0
        },
        "sources": {
          "description": "The sources themselves. Every one listed is active; remove it to stop reading it.",
          "type": "array",
          "default": [],
          "items": {
            "$ref": "#/$defs/Source"
          }
        }
      },
      "additionalProperties": false
    },
    "Chip": {
      "description": "One chip the system-monitor applet can show on the bar. Disk is deliberately not a variant —\narbitrarily many configured paths don't reduce to one chip value, so disk stays popover-only.",
      "type": "string",
      "enum": [
        "cpu",
        "ram",
        "swap",
        "network",
        "gpu"
      ]
    },
    "Clipboard": {
      "description": "What the panel remembers of the clipboard. The history lives in the panel process and in memory\nonly: it survives nothing, which is deliberate for a buffer that routinely holds passwords.",
      "type": "object",
      "properties": {
        "capture-images": {
          "description": "Whether images are captured as well as text. Off records text only; an image copied while\noff is not recorded at all rather than recorded without its content.",
          "type": "boolean",
          "default": true
        },
        "enabled": {
          "description": "Whether the clipboard is watched at all. Off means no compositor connection is opened and\nnothing is recorded; the applet then renders as if nothing had ever been copied.",
          "type": "boolean",
          "default": true
        },
        "limit": {
          "description": "How many entries are kept. Pinned entries do not count against it and are never evicted, so\na history full of pins holds more than this many. Clamped to 1..=10000.",
          "type": "integer",
          "format": "uint",
          "default": 100,
          "minimum": 0
        },
        "max-entry-kib": {
          "description": "The largest single entry that will be captured, in kibibytes. Another application chooses\nwhat it puts on the clipboard, so this is what stops one handing over something enormous.\nClamped to 4..=2048: the compositor read is capped at 2 MiB, and a larger value here would\npromise a size that is discarded before it ever reaches the history.",
          "type": "integer",
          "format": "uint32",
          "default": 2048,
          "minimum": 0
        },
        "max-total-kib": {
          "description": "The total the history may occupy, in kibibytes. Reached, the oldest unpinned entry is\ndropped. This bounds memory in a way `limit` alone cannot, because one image can outweigh a\nhundred lines of text. Clamped to 64..=1048576.",
          "type": "integer",
          "format": "uint32",
          "default": 10240,
          "minimum": 0
        }
      },
      "additionalProperties": false
    },
    "Clock": {
      "type": "object",
      "properties": {
        "date-format": {
          "description": "`{day}` is the day of the month with its ordinal suffix, e.g. \"3rd\".",
          "type": "string",
          "default": "%A, {day} %B"
        },
        "enabled": {
          "type": "boolean",
          "default": true
        },
        "time-format": {
          "type": "string",
          "default": "%H:%M"
        }
      },
      "additionalProperties": false
    },
    "ColorFormat": {
      "oneOf": [
        {
          "description": "`#E0563F`",
          "type": "string",
          "const": "hex"
        },
        {
          "description": "`rgb(224 86 63)`",
          "type": "string",
          "const": "rgb"
        },
        {
          "description": "`hsl(9 72% 56%)`",
          "type": "string",
          "const": "hsl"
        },
        {
          "description": "`hsv(9 72% 88%)`",
          "type": "string",
          "const": "hsv"
        },
        {
          "description": "`oklch(0.63 0.18 32)`",
          "type": "string",
          "const": "oklch"
        },
        {
          "description": "`cmyk(0% 62% 72% 12%)`, a naive conversion with no color profile.",
          "type": "string",
          "const": "cmyk"
        }
      ]
    },
    "ColorPicker": {
      "description": "The screen color picker. `glimpse-picker` reads it for the lens and the notation it prints;\nthe panel reads it for the palette, which lives in the panel process and in memory only.",
      "type": "object",
      "properties": {
        "format": {
          "description": "The notation a picked color is copied in, and the one the palette shows it in. Every other\nnotation stays one click away in the palette.",
          "$ref": "#/$defs/ColorFormat",
          "default": "hex"
        },
        "lens-radius": {
          "description": "The lens's radius in logical pixels. Clamped to 40..=400.",
          "type": "integer",
          "format": "uint32",
          "default": 106,
          "minimum": 0
        },
        "limit": {
          "description": "How many colors the palette keeps, newest first. A pick beyond it drops the oldest.\nClamped to 1..=64.",
          "type": "integer",
          "format": "uint",
          "default": 8,
          "minimum": 0
        },
        "max-zoom": {
          "description": "How far the scroll wheel zooms the lens in, as a magnification: `30` shows one screen\npixel thirty logical pixels wide. The lens opens at x8, or at this if it is lower.\nClamped to 2..=64.",
          "type": "integer",
          "format": "uint32",
          "default": 30,
          "minimum": 0
        }
      },
      "additionalProperties": false
    },
    "ColorScheme": {
      "type": "string",
      "enum": [
        "light",
        "dark",
        "auto"
      ]
    },
    "FirstDay": {
      "description": "Which day a calendar week starts on. There is deliberately no `locale` here, unlike\n`hour-format`: the two ways to ask the system are GTK's translated `calendar:week_start:0`,\nwhich came back untranslated when it was measured and would have answered Sunday under an\n`LC_TIME` that means Monday, and glibc's `_NL_TIME_FIRST_WEEKDAY`, which is not portable. A\n`locale` that quietly answers wrong is worse than a default that says what it is.",
      "type": "string",
      "enum": [
        "monday",
        "sunday"
      ]
    },
    "Fit": {
      "oneOf": [
        {
          "type": "string",
          "enum": [
            "cover",
            "contain",
            "fill"
          ]
        },
        {
          "description": "The image at its own size, centred on color, downscaled only when it exceeds the\noutput. contain enlarges a small image and looks soft; this is the honest option.",
          "type": "string",
          "const": "center"
        }
      ]
    },
    "Geolocation": {
      "description": "`manual` carries its coordinates in the table that selects it, so a provider without them cannot\nbe written down: serde refuses the document and names the key that is missing. The alternative —\ntwo loose `Option` fields — makes a half-filled table a runtime problem for every reader.",
      "oneOf": [
        {
          "type": "object",
          "properties": {
            "provider": {
              "type": "string",
              "const": "geoclue"
            }
          },
          "required": [
            "provider"
          ]
        },
        {
          "type": "object",
          "properties": {
            "latitude": {
              "type": "number",
              "format": "double"
            },
            "longitude": {
              "type": "number",
              "format": "double"
            },
            "provider": {
              "type": "string",
              "const": "manual"
            }
          },
          "required": [
            "provider",
            "latitude",
            "longitude"
          ]
        }
      ]
    },
    "HourFormat": {
      "description": "How a time of day reads.",
      "oneOf": [
        {
          "description": "Follow `LC_TIME`, which is what the locale's own `%X` resolves to.",
          "type": "string",
          "const": "locale"
        },
        {
          "description": "`3:30 PM`.",
          "type": "string",
          "const": "12h"
        },
        {
          "description": "`15:30`.",
          "type": "string",
          "const": "24h"
        }
      ]
    },
    "Idle": {
      "type": "object",
      "properties": {
        "enabled": {
          "type": "boolean",
          "default": true
        },
        "profiles": {
          "$ref": "#/$defs/Profiles",
          "default": {
            "ac": {
              "listeners": [
                {
                  "on-idle": "loginctl lock-session",
                  "on-resume": "",
                  "timeout": 900
                },
                {
                  "on-idle": "systemctl suspend",
                  "on-resume": "",
                  "timeout": 3600
                }
              ]
            },
            "battery": {
              "listeners": [
                {
                  "on-idle": "loginctl lock-session",
                  "on-resume": "",
                  "timeout": 900
                },
                {
                  "on-idle": "systemctl suspend",
                  "on-resume": "",
                  "timeout": 1800
                }
              ]
            }
          }
        },
        "respect-inhibitors": {
          "type": "boolean",
          "default": true
        }
      },
      "additionalProperties": false
    },
    "Keyboard": {
      "type": "object",
      "properties": {
        "labels": {
          "type": "object",
          "additionalProperties": {
            "type": "string"
          },
          "default": {}
        },
        "remember": {
          "$ref": "#/$defs/Remember",
          "default": "window"
        }
      },
      "additionalProperties": false
    },
    "Listener": {
      "type": "object",
      "properties": {
        "on-idle": {
          "type": "string",
          "default": ""
        },
        "on-resume": {
          "type": "string",
          "default": ""
        },
        "respect-inhibitors": {
          "type": [
            "boolean",
            "null"
          ]
        },
        "timeout": {
          "type": "integer",
          "format": "uint64",
          "default": 0,
          "minimum": 0
        }
      },
      "additionalProperties": false
    },
    "Lock": {
      "type": "object",
      "properties": {
        "background": {
          "$ref": "#/$defs/LockBackground",
          "default": {
            "blur-radius": 0,
            "color": "#000000",
            "dim": 0.35,
            "dim-dark": 0.5,
            "fit": "cover"
          }
        },
        "clock": {
          "$ref": "#/$defs/Clock",
          "default": {
            "date-format": "%A, {day} %B",
            "enabled": true,
            "time-format": "%H:%M"
          }
        },
        "media": {
          "description": "The now-playing transport, when something is playing.",
          "$ref": "#/$defs/LockSection",
          "default": {
            "enabled": true
          }
        },
        "notifications": {
          "description": "Notifications received while the screen is locked.",
          "$ref": "#/$defs/LockNotifications",
          "default": {
            "enabled": true,
            "privacy": "apps"
          }
        },
        "pam-service": {
          "type": "string",
          "default": "glimpse-lock"
        },
        "prompt-output": {
          "description": "`\"focused\"` follows the compositor's focused output; anything else is a connector name.",
          "type": "string",
          "default": "focused"
        },
        "session": {
          "description": "The suspend, reboot and power-off actions offered from the lock stage.",
          "$ref": "#/$defs/LockSession",
          "default": {
            "actions": [
              "suspend",
              "reboot",
              "power-off"
            ],
            "enabled": true
          }
        },
        "status": {
          "description": "The status island: weather, battery, keyboard layout, bluetooth and network.",
          "$ref": "#/$defs/LockSection",
          "default": {
            "enabled": true
          }
        }
      },
      "additionalProperties": false
    },
    "LockBackground": {
      "description": "The lock stage's background: an image over a color, blurred and dimmed.",
      "type": "object",
      "properties": {
        "blur-radius": {
          "description": "Gaussian blur radius in output pixels; 0 blurs nothing.",
          "type": "integer",
          "format": "uint32",
          "default": 0,
          "maximum": 128,
          "minimum": 0
        },
        "color": {
          "description": "Painted under everything: the whole background when no image resolves, and what shows\nthrough `contain`'s letterbox.",
          "type": "string",
          "default": "#000000"
        },
        "dim": {
          "description": "How much the background is darkened, from 0 (untouched) to 1 (black).",
          "type": "number",
          "format": "double",
          "default": 0.35,
          "maximum": 1.0,
          "minimum": 0.0
        },
        "dim-dark": {
          "description": "Overrides `dim` under the dark color scheme.",
          "type": "number",
          "format": "double",
          "default": 0.5,
          "maximum": 1.0,
          "minimum": 0.0
        },
        "fit": {
          "description": "How the image meets an output of a different aspect ratio.",
          "$ref": "#/$defs/Fit",
          "default": "cover"
        },
        "image": {
          "description": "The background image, used as-is or under the light color scheme when `image-dark` is set.\nWith neither this nor `image-dark` set, the lock shows `[wallpaper]`'s `image` and\n`image-dark`.",
          "type": [
            "string",
            "null"
          ]
        },
        "image-dark": {
          "description": "The background image under the dark color scheme. Falls back to `image` when unset.",
          "type": [
            "string",
            "null"
          ]
        }
      },
      "additionalProperties": false
    },
    "LockNotifications": {
      "description": "Which notifications reach the lock stage, and how much of each one is shown.",
      "type": "object",
      "properties": {
        "enabled": {
          "description": "Whether notifications show on the lock stage at all.",
          "type": "boolean",
          "default": true
        },
        "privacy": {
          "description": "A locked screen never shows a notification's body. `count` shows only a total; `apps`\nalso names which applications sent them.",
          "$ref": "#/$defs/LockPrivacy",
          "default": "apps"
        }
      },
      "additionalProperties": false
    },
    "LockPrivacy": {
      "description": "How much of a notification's own content shows on the lock stage.",
      "oneOf": [
        {
          "description": "Only a total count, naming nothing about any individual notification.",
          "type": "string",
          "const": "count"
        },
        {
          "description": "Also names which application each notification came from, never a summary or body.",
          "type": "string",
          "const": "apps"
        }
      ]
    },
    "LockSection": {
      "description": "A lock stage element with nothing to configure besides whether it shows at all.",
      "type": "object",
      "properties": {
        "enabled": {
          "description": "Whether this element shows on the lock stage at all.",
          "type": "boolean",
          "default": true
        }
      },
      "additionalProperties": false
    },
    "LockSession": {
      "description": "The session actions offered from the lock stage.",
      "type": "object",
      "properties": {
        "actions": {
          "description": "Every action here is also gated by logind: one it refuses does not appear even when listed.",
          "type": "array",
          "default": [
            "suspend",
            "reboot",
            "power-off"
          ],
          "items": {
            "$ref": "#/$defs/LockSessionAction"
          }
        },
        "enabled": {
          "description": "Whether the session actions show on the lock stage at all.",
          "type": "boolean",
          "default": true
        }
      },
      "additionalProperties": false
    },
    "LockSessionAction": {
      "description": "One system action offered from the lock stage.",
      "oneOf": [
        {
          "description": "Suspend the system.",
          "type": "string",
          "const": "suspend"
        },
        {
          "description": "Reboot the system.",
          "type": "string",
          "const": "reboot"
        },
        {
          "description": "Power the system off.",
          "type": "string",
          "const": "power-off"
        }
      ]
    },
    "Margin": {
      "type": "object",
      "properties": {
        "bottom": {
          "type": "integer",
          "format": "uint32",
          "default": 0,
          "minimum": 0
        },
        "left": {
          "type": "integer",
          "format": "uint32",
          "default": 0,
          "minimum": 0
        },
        "right": {
          "type": "integer",
          "format": "uint32",
          "default": 0,
          "minimum": 0
        },
        "top": {
          "type": "integer",
          "format": "uint32",
          "default": 0,
          "minimum": 0
        }
      },
      "additionalProperties": false
    },
    "Monitors": {
      "type": "object",
      "properties": {
        "builtin-connector": {
          "type": [
            "string",
            "null"
          ]
        }
      },
      "additionalProperties": false
    },
    "Mpris": {
      "description": "Which media players the panel service follows, and what it does with their artwork. There are no\nplayers here: every `org.mpris.MediaPlayer2.*` on the session bus is followed unless `ignore`\nsays otherwise.",
      "type": "object",
      "properties": {
        "art-max-kib": {
          "description": "The largest artwork that will be downloaded, in kibibytes. A player chooses this URL, so the\ncap is what stops one pointing the service at something enormous. It bounds downloads only;\nartwork a player names as a local file is bounded by its pixel dimensions instead, when it is\ndecoded. Clamped to 16..=65536.",
          "type": "integer",
          "format": "uint32",
          "default": 4096,
          "minimum": 0
        },
        "fetch-art": {
          "description": "Whether artwork named by an `http` or `https` URL is downloaded. Players that name a local\nfile are unaffected, and turning this off leaves those working. Downloads go to\n`$XDG_RUNTIME_DIR/glimpse/art/` and are capped by `art-max-kib`.",
          "type": "boolean",
          "default": true
        },
        "ignore": {
          "description": "Regular expressions matched against a player's bus-name suffix (`spotify`,\n`kdeconnect.mpris_desktop_1`) and its `Identity` (`Spotify`, `Firefox`). A player matching\nany of them is never shown. Patterns are unanchored and case-sensitive; write `(?i)` for\ncase-insensitive and `^…$` to anchor. A pattern that does not compile is skipped with a\nwarning and the rest keep working. `playerctld` and KDE Connect mirrors are already\nhandled without this.\n\nTo start from: `'^chromium'` hides every Chromium tab, whose bus names carry an instance\nnumber after the application (`chromium.instance4181`); `'(?i)firefox'` hides Firefox\nhowever it happens to capitalize its `Identity`; `'^mpv$'` hides exactly `mpv` and leaves\n`mpv-shim` alone. Write them as TOML literal strings, in single quotes, so a backslash\nreaches the regex instead of being read as a string escape:\n`ignore = ['^chromium', '(?i)firefox']`.",
          "type": "array",
          "default": [],
          "items": {
            "type": "string"
          }
        }
      },
      "additionalProperties": false
    },
    "Network": {
      "type": "object",
      "properties": {
        "hide-unnamed": {
          "description": "Whether to hide networks that broadcast no name. They cannot be joined by tapping them —\na hidden network is reached by typing its name in — so a row for one is unactionable.",
          "type": "boolean",
          "default": true
        },
        "scan-timeout": {
          "description": "How many seconds a scan for nearby networks runs before stopping on its own. Scanning\ninterrupts traffic on the same radio, so it is never left running. `0` disables the timeout\nand leaves the scan running until the popover closes.",
          "type": "integer",
          "format": "uint64",
          "default": 30,
          "minimum": 0
        },
        "show-vpn": {
          "description": "Whether to show the VPN section. It hides itself when no VPN profile is saved, so this is\nfor hiding one you have but do not want on the list.",
          "type": "boolean",
          "default": true
        }
      },
      "additionalProperties": false
    },
    "NightLight": {
      "type": "object",
      "properties": {
        "end-time": {
          "type": [
            "string",
            "null"
          ],
          "pattern": "^([01]?[0-9]|2[0-3]):[0-5]?[0-9]$"
        },
        "schedule": {
          "$ref": "#/$defs/Schedule",
          "default": "automatic"
        },
        "start-time": {
          "type": [
            "string",
            "null"
          ],
          "pattern": "^([01]?[0-9]|2[0-3]):[0-5]?[0-9]$"
        },
        "temperature": {
          "type": "integer",
          "format": "uint32",
          "default": 4200,
          "minimum": 0
        },
        "transition-minutes": {
          "type": "integer",
          "format": "uint32",
          "default": 15,
          "minimum": 0
        }
      },
      "additionalProperties": false
    },
    "NotificationEdge": {
      "type": "string",
      "enum": [
        "top-left",
        "top-center",
        "top-right",
        "bottom-left",
        "bottom-center",
        "bottom-right"
      ]
    },
    "NotificationIndicatorStyle": {
      "type": "string",
      "enum": [
        "icon-only",
        "icon-dot",
        "icon-counter"
      ]
    },
    "Notifications": {
      "type": "object",
      "properties": {
        "edge": {
          "$ref": "#/$defs/NotificationEdge",
          "default": "top-right"
        },
        "enabled": {
          "type": "boolean",
          "default": true
        },
        "hide-delay": {
          "type": "integer",
          "format": "uint64",
          "default": 4,
          "maximum": 86400,
          "minimum": 1
        },
        "keep": {
          "description": "How many notifications are kept once they have been read. Dismissing one puts it here\nrather than destroying it, which is what makes an accidental dismissal recoverable; past\nthis many, the oldest is dropped for good. Senders choose how often they notify, so this\nis the bound that stops a chatty one growing the provider's memory without limit. Clamped\nto 1..=1000.",
          "type": "integer",
          "format": "uint32",
          "default": 100,
          "minimum": 0
        },
        "max-items": {
          "type": "integer",
          "format": "uint32",
          "default": 6,
          "minimum": 1
        },
        "monitor": {
          "type": [
            "string",
            "null"
          ],
          "default": null
        },
        "suppress": {
          "description": "Regex patterns matched against the application identity, application name, title and body.\nMatching notifications are not stored or published.",
          "type": "array",
          "default": [],
          "items": {
            "type": "string"
          }
        }
      },
      "additionalProperties": false
    },
    "PagerMode": {
      "description": "What each slot stands for: `workspaces` for one slot per workspace, `windows` for one slot per\nwindow on the current workspace.",
      "type": "string",
      "enum": [
        "workspaces",
        "windows"
      ]
    },
    "PagerScope": {
      "description": "How much of the session the strip covers: `current` for only the workspace the user is on,\n`output` for the workspaces on this panel's monitor, `session` for every workspace on every\nmonitor.",
      "type": "string",
      "enum": [
        "current",
        "output",
        "session"
      ]
    },
    "PagerShape": {
      "description": "How a slot is drawn: `dots` for a dot each with the current one drawn longer, which takes\nthe least room on the bar, or `labels` for the slot's label in a pill.",
      "type": "string",
      "enum": [
        "dots",
        "labels"
      ]
    },
    "Panel": {
      "type": "object",
      "properties": {
        "center": {
          "type": "array",
          "default": [
            "clock",
            "weather",
            "notifications",
            "privacy"
          ],
          "items": {
            "type": "string"
          }
        },
        "left": {
          "type": "array",
          "default": [
            "workspace-name",
            "pager",
            "places",
            "mpris"
          ],
          "items": {
            "type": "string"
          }
        },
        "margin": {
          "$ref": "#/$defs/Margin",
          "default": {
            "bottom": 0,
            "left": 0,
            "right": 0,
            "top": 0
          }
        },
        "monitor": {
          "type": [
            "string",
            "null"
          ]
        },
        "position": {
          "$ref": "#/$defs/Position",
          "default": "top"
        },
        "right": {
          "type": "array",
          "default": [
            "next-event",
            "tray",
            "removable",
            "clipboard",
            "color-picker",
            "printing",
            "bluetooth",
            "network",
            "display",
            "brightness",
            "audio",
            "idle",
            "battery",
            "keyboard",
            "session"
          ],
          "items": {
            "type": "string"
          }
        },
        "size": {
          "type": "integer",
          "format": "uint32",
          "default": 36,
          "minimum": 0
        }
      },
      "additionalProperties": false
    },
    "Place": {
      "description": "Where a weather applet looks. Tagged on `at`, matching the shape `WatchPlace` takes, so a\nplace reads the same in a document and on the wire.",
      "oneOf": [
        {
          "description": "Follow the geolocation service, and move with it.",
          "type": "object",
          "properties": {
            "at": {
              "type": "string",
              "const": "here"
            }
          },
          "additionalProperties": false,
          "required": [
            "at"
          ]
        },
        {
          "description": "A fixed pair of coordinates in degrees.",
          "type": "object",
          "properties": {
            "at": {
              "type": "string",
              "const": "latlon"
            },
            "latitude": {
              "description": "Degrees north of the equator, between -90 and 90.",
              "type": "number",
              "format": "double"
            },
            "longitude": {
              "description": "Degrees east of Greenwich, between -180 and 180.",
              "type": "number",
              "format": "double"
            }
          },
          "additionalProperties": false,
          "required": [
            "at",
            "latitude",
            "longitude"
          ]
        },
        {
          "description": "Resolve a city and ISO 3166-1 alpha-2 country code through Open-Meteo. This sends a\ngeocoding request to Open-Meteo, which receives the machine's public IP address.",
          "type": "object",
          "properties": {
            "at": {
              "type": "string",
              "const": "location"
            },
            "name": {
              "description": "City and country code, written as `City, CC`.",
              "type": "string"
            }
          },
          "additionalProperties": false,
          "required": [
            "at",
            "name"
          ]
        },
        {
          "description": "A fixed pair of coordinates in degrees.",
          "type": "object",
          "properties": {
            "at": {
              "type": "string",
              "const": "coordinates"
            },
            "latitude": {
              "description": "Degrees north of the equator, between -90 and 90.",
              "type": "number",
              "format": "double"
            },
            "longitude": {
              "description": "Degrees east of Greenwich, between -180 and 180.",
              "type": "number",
              "format": "double"
            }
          },
          "additionalProperties": false,
          "required": [
            "at",
            "latitude",
            "longitude"
          ]
        }
      ]
    },
    "Places": {
      "type": "object",
      "properties": {
        "network": {
          "description": "Whether network shares are watched and listed. Turns off one directory read.",
          "type": "boolean",
          "default": true
        },
        "trash": {
          "description": "Whether the trash is watched and listed. Turns off one persistent inotify watch.",
          "type": "boolean",
          "default": true
        }
      },
      "additionalProperties": false
    },
    "Position": {
      "type": "string",
      "enum": [
        "left",
        "top",
        "right",
        "bottom"
      ]
    },
    "Power": {
      "type": "object",
      "properties": {
        "lock-before-sleep": {
          "type": "boolean",
          "default": true
        },
        "lock-on-request": {
          "type": "boolean",
          "default": true
        }
      },
      "additionalProperties": false
    },
    "Printing": {
      "description": "The printing service: how often it polls CUPS for job state, and which server it polls.",
      "type": "object",
      "properties": {
        "poll-active": {
          "description": "How often the service polls CUPS while a job is active, in seconds. Must be at least 1;\n`0` would leave the poll timer with no interval to tick on.",
          "type": "integer",
          "format": "uint64",
          "default": 2,
          "minimum": 1
        },
        "poll-idle": {
          "description": "How often the service polls CUPS while no job is active, in seconds. Must be at least 1,\nfor the same reason as `poll-active`.",
          "type": "integer",
          "format": "uint64",
          "default": 30,
          "minimum": 1
        },
        "server-url": {
          "description": "The CUPS server to poll, such as `http://localhost:631/`. Unset uses CUPS's own default\nserver.",
          "type": [
            "string",
            "null"
          ]
        }
      },
      "additionalProperties": false
    },
    "Profile": {
      "type": "object",
      "properties": {
        "listeners": {
          "type": "array",
          "default": [],
          "items": {
            "$ref": "#/$defs/Listener"
          }
        }
      },
      "additionalProperties": false
    },
    "Profiles": {
      "type": "object",
      "properties": {
        "ac": {
          "$ref": "#/$defs/Profile",
          "default": {
            "listeners": [
              {
                "on-idle": "loginctl lock-session",
                "on-resume": "",
                "timeout": 900
              },
              {
                "on-idle": "systemctl suspend",
                "on-resume": "",
                "timeout": 3600
              }
            ]
          }
        },
        "battery": {
          "$ref": "#/$defs/Profile",
          "default": {
            "listeners": [
              {
                "on-idle": "loginctl lock-session",
                "on-resume": "",
                "timeout": 900
              },
              {
                "on-idle": "systemctl suspend",
                "on-resume": "",
                "timeout": 1800
              }
            ]
          }
        }
      },
      "additionalProperties": false
    },
    "Provider": {
      "description": "Where a forecast comes from.",
      "oneOf": [
        {
          "description": "Open-Meteo, which needs no account and no key.",
          "type": "string",
          "const": "open-meteo"
        },
        {
          "description": "The Norwegian Meteorological Institute, which needs no key either but asks for a\nUser-Agent naming the application. It is the only source that carries official warnings.",
          "type": "string",
          "const": "met-no"
        }
      ]
    },
    "Regional": {
      "description": "What language this session reads, and how it writes a time of day and a measurement. Every\nsetting here defaults to what the environment already says, so an empty table is the right\nanswer on a machine whose locale is set up.\n\nThe three keys are read by different owners: `glimpse-weather` resolves `units` and stamps the\nanswer onto every weather reading, while the panel, the lock screen and the wallpaper resolve\n`language` and `hour-format` for themselves. Nothing else in glimpse asks the environment\nthese questions.",
      "type": "object",
      "properties": {
        "hour-format": {
          "description": "How a time of day reads wherever glimpse composes one itself — the world clock, the times\ninside event rows, sunrise and sunset. A `strftime` format you write yourself, such as the\nclock applet's `label-format`, is yours and is not affected by this.",
          "$ref": "#/$defs/HourFormat",
          "default": "locale"
        },
        "language": {
          "description": "Which translation catalog the interface is drawn in, such as `ru`. Empty follows the\nenvironment, which is `LANGUAGE`, then `LC_MESSAGES`, then `LANG`. Naming one here sets\n`LANGUAGE` for the process, so it applies whether or not that locale has been generated.\n\nThis chooses the catalog and nothing else: weekday names, the twelve-hour question and the\nunit system keep following the environment, because a person reading a Russian interface in\nChicago still wants their own clock and their own thermometer.\n\nA change here takes effect when the binary next starts. A GTK template resolves its text as\neach widget is built, so re-reading it in a running process would leave what is already on\nscreen in the old language and everything opened afterwards in the new one.",
          "type": "string",
          "default": ""
        },
        "units": {
          "description": "Whether temperatures, wind speeds and rainfall are reported in metric or imperial units.\nEvery reading in one weather report uses the same system, and the panel prints the symbols\nthat go with it.",
          "$ref": "#/$defs/Units",
          "default": "locale"
        }
      },
      "additionalProperties": false
    },
    "Remember": {
      "type": "string",
      "enum": [
        "global",
        "app",
        "window"
      ]
    },
    "Removable": {
      "type": "object",
      "properties": {
        "capacity-interval": {
          "description": "Seconds between free-space samples of mounted removable volumes. `0` turns the capacity\nreadout off and runs no timer.",
          "type": "integer",
          "format": "uint64",
          "default": 10,
          "minimum": 0
        }
      },
      "additionalProperties": false
    },
    "Ruler": {
      "description": "The screen ruler. `glimpse-ruler` reads it for the lens and the history it keeps.",
      "type": "object",
      "properties": {
        "history-limit": {
          "description": "How many measurements history keeps, newest first. A measurement beyond it drops the\noldest. Clamped to 1..=64.",
          "type": "integer",
          "format": "uint",
          "default": 8,
          "minimum": 0
        },
        "lens-radius": {
          "description": "The lens's radius in logical pixels. Clamped to 40..=400.",
          "type": "integer",
          "format": "uint32",
          "default": 106,
          "minimum": 0
        },
        "max-zoom": {
          "description": "How far the scroll wheel zooms the lens in, as a magnification. Clamped to 2..=64.",
          "type": "integer",
          "format": "uint32",
          "default": 30,
          "minimum": 0
        }
      },
      "additionalProperties": false
    },
    "Schedule": {
      "type": "string",
      "enum": [
        "off",
        "automatic",
        "schedule"
      ]
    },
    "Source": {
      "description": "One calendar to read.",
      "type": "object",
      "properties": {
        "color": {
          "description": "The dot beside this calendar's events, as a hex color.",
          "type": [
            "string",
            "null"
          ]
        },
        "id": {
          "description": "What this source is called internally. It has to be unique, and it is what an event\ncarries so a surface can tell which calendar it came from.",
          "type": "string"
        },
        "name": {
          "description": "What to call this calendar on screen. Unset falls back to `id`.",
          "type": [
            "string",
            "null"
          ]
        },
        "poll-interval": {
          "description": "How often to fetch this one, in seconds, overriding the shared interval. Ignored by a\nsource that is watched rather than fetched, the way the shared one is.",
          "type": [
            "integer",
            "null"
          ],
          "format": "uint64",
          "minimum": 0
        },
        "type": {
          "description": "Whether `uri` names a subscription feed or a directory of `.ics` files.",
          "$ref": "#/$defs/SourceKind"
        },
        "uri": {
          "description": "The feed or directory. `webcal://`, which is what a provider's Subscribe button hands\nout, is read as the `https://` it stands for. A provider's iCalendar URL behaves like a\nread-only access token — anyone holding it can usually read the calendar without signing\nin — so a `file://` path to a one-line sidecar file is offered as an alternative to\nwriting a secret URL into a configuration file that gets shared or committed.",
          "type": "string"
        }
      },
      "additionalProperties": false,
      "required": [
        "id",
        "type",
        "uri"
      ]
    },
    "SourceKind": {
      "description": "What a source's `uri` points at.",
      "oneOf": [
        {
          "description": "One iCalendar document: an `https://` or `webcal://` feed, a local `.ics`, or a `file://`\nholding a one-line feed URL. A local one is watched; a remote one is fetched on\n`poll-interval`.",
          "type": "string",
          "const": "ical"
        },
        {
          "description": "A directory of `.ics` files, watched for changes rather than polled.",
          "type": "string",
          "const": "directory"
        }
      ]
    },
    "SystemMonitor": {
      "description": "The system-monitor service: what it samples and how often. Whether it samples at all is not a\nkey here — it is derived from panel placement (see `placed_kinds`), because a poll interval\nonly means something once something is actually placed to consume it.",
      "type": "object",
      "properties": {
        "disk-paths": {
          "description": "Filesystem paths sampled for free space, each becoming its own popover tile. A path that\ndoes not exist is skipped. An unmounted-but-present path still resolves — `statvfs` reports\nwhichever filesystem currently owns the directory, the same way `df` would — accepted, not\na bug. Each entry must be absolute; duplicates (after stripping a trailing `/`) collapse to\ntheir first occurrence.",
          "type": "array",
          "default": [
            "/"
          ],
          "items": {
            "type": "string"
          }
        },
        "gpu": {
          "description": "Whether to probe for an amdgpu GPU at all.",
          "type": "boolean",
          "default": true
        },
        "poll-interval": {
          "description": "Seconds between samples of every reading this service publishes. Must be between 1 and 60.",
          "type": "integer",
          "format": "uint64",
          "default": 2,
          "maximum": 60,
          "minimum": 1
        }
      },
      "additionalProperties": false
    },
    "Timezone": {
      "description": "One zone in the clock popover's world clock.",
      "type": "object",
      "properties": {
        "icon": {
          "description": "An icon name replacing the sun or moon the row picks from the hour it is showing.",
          "type": [
            "string",
            "null"
          ]
        },
        "label": {
          "description": "What the row is called, such as `Tokyo`.",
          "type": "string"
        },
        "note": {
          "description": "A note after the `tomorrow` or `yesterday` the row works out for itself, such as\n`the office`.",
          "type": [
            "string",
            "null"
          ]
        },
        "timezone": {
          "description": "The IANA zone the row reads, such as `Asia/Tokyo`.",
          "type": "string"
        }
      },
      "additionalProperties": false,
      "required": [
        "label",
        "timezone"
      ]
    },
    "Transition": {
      "description": "How one wallpaper image replaces another.",
      "oneOf": [
        {
          "description": "A cut, with no animation.",
          "type": "string",
          "const": "none"
        },
        {
          "description": "A crossfade between the outgoing and incoming image.",
          "type": "string",
          "const": "fade"
        }
      ]
    },
    "Units": {
      "description": "Which units every reading is reported in.",
      "oneOf": [
        {
          "description": "Follow `LC_MEASUREMENT`. Every locale glibc ships is metric except `en_US`, so this is\nmetric unless the machine is set to American English.",
          "type": "string",
          "const": "locale"
        },
        {
          "description": "Celsius, kilometres per hour, millimetres.",
          "type": "string",
          "const": "metric"
        },
        {
          "description": "Fahrenheit, miles per hour, inches.",
          "type": "string",
          "const": "imperial"
        }
      ]
    },
    "Wallpaper": {
      "type": "object",
      "properties": {
        "backdrop": {
          "$ref": "#/$defs/Backdrop",
          "default": {
            "blur-radius": 24,
            "downscale-factor": 4,
            "enabled": true
          }
        },
        "color": {
          "description": "Painted under everything: the whole wallpaper when no image resolves, and what shows\nthrough `contain`'s letterbox.",
          "type": "string",
          "default": "#000000"
        },
        "fit": {
          "description": "How the image meets an output of a different aspect ratio.",
          "$ref": "#/$defs/Fit",
          "default": "cover"
        },
        "image": {
          "description": "The wallpaper image, used as-is or under the light color scheme when `image-dark` is set.",
          "type": [
            "string",
            "null"
          ],
          "default": "/usr/share/glimpse/themes/adwaita/default.jpg"
        },
        "image-dark": {
          "description": "The wallpaper image under the dark color scheme. Falls back to `image` when unset.",
          "type": [
            "string",
            "null"
          ]
        },
        "outputs": {
          "description": "Per-output overrides, matched against the connector `gdk::Monitor::connector()` returns\n(the same string `[[panels]].monitor` matches against). An output with no entry here\nrenders the values above. When two entries name the same connector, the first one wins.",
          "type": "array",
          "default": [],
          "items": {
            "$ref": "#/$defs/WallpaperOutput"
          }
        },
        "transition": {
          "description": "How one image replaces another.",
          "$ref": "#/$defs/Transition",
          "default": "fade"
        },
        "transition-ms": {
          "description": "Duration of that transition, in milliseconds.",
          "type": "integer",
          "format": "uint32",
          "default": 500,
          "maximum": 5000,
          "minimum": 0
        }
      },
      "additionalProperties": false
    },
    "WallpaperOutput": {
      "description": "A per-output override of the wallpaper (and backdrop) shown on one connector. Every field but\n`monitor` is optional and falls back to the corresponding value in `[wallpaper]`, or\n`[wallpaper.backdrop]` for the nested `backdrop` fields, when unset.",
      "type": "object",
      "properties": {
        "backdrop": {
          "description": "Overrides `[wallpaper.backdrop]` for this connector.",
          "anyOf": [
            {
              "$ref": "#/$defs/BackdropOutput"
            },
            {
              "type": "null"
            }
          ]
        },
        "color": {
          "description": "Overrides `[wallpaper] color` for this connector.",
          "type": [
            "string",
            "null"
          ]
        },
        "fit": {
          "description": "Overrides `[wallpaper] fit` for this connector.",
          "anyOf": [
            {
              "$ref": "#/$defs/Fit"
            },
            {
              "type": "null"
            }
          ]
        },
        "image": {
          "description": "Overrides `[wallpaper] image` for this connector.",
          "type": [
            "string",
            "null"
          ]
        },
        "image-dark": {
          "description": "Overrides `[wallpaper] image-dark` for this connector.",
          "type": [
            "string",
            "null"
          ]
        },
        "monitor": {
          "description": "The connector this override applies to, e.g. `\"DP-2\"`.",
          "type": "string"
        },
        "transition": {
          "description": "Overrides `[wallpaper] transition` for this connector.",
          "anyOf": [
            {
              "$ref": "#/$defs/Transition"
            },
            {
              "type": "null"
            }
          ]
        },
        "transition-ms": {
          "description": "Overrides `[wallpaper] transition-ms` for this connector.",
          "type": [
            "integer",
            "null"
          ],
          "format": "uint32",
          "maximum": 5000,
          "minimum": 0
        }
      },
      "additionalProperties": false,
      "required": [
        "monitor"
      ]
    },
    "Weather": {
      "description": "How a forecast is fetched. There are no places here: a place is registered by whatever wants to\nsee it, through `WatchPlace`, and held for as long as it keeps asking. Nothing is fetched and\nno location is resolved until something does.",
      "type": "object",
      "properties": {
        "forecast-days": {
          "description": "How many days the daily forecast covers, today included. Clamped to 1..=10. The panel's\nlist starts at tomorrow, so it shows one fewer day than this asks for.",
          "type": "integer",
          "format": "uint8",
          "default": 7,
          "maximum": 255,
          "minimum": 0
        },
        "poll-interval": {
          "description": "How often a forecast is fetched again, in seconds. The provider recomputes current\nconditions every fifteen minutes, so anything shorter asks again for data that has not\nmoved; the provider's `Refresh` method is how a person asks for it now. Values below 600 are raised.",
          "type": "integer",
          "format": "uint64",
          "default": 900,
          "minimum": 0
        },
        "provider": {
          "description": "Which service a forecast comes from. `open-meteo` needs no account and no key.",
          "$ref": "#/$defs/Provider",
          "default": "open-meteo"
        }
      },
      "additionalProperties": false
    }
  }
}
