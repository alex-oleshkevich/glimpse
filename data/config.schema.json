{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "Config",
  "type": "object",
  "properties": {
    "appearance": {
      "$ref": "#/$defs/Appearance",
      "default": {
        "color-scheme": "auto",
        "theme": "adwaita",
        "theme-variant": ""
      }
    },
    "applets": {
      "type": "object",
      "properties": {
        "audio": {
          "description": "Output volume, with the default sink and per-application streams in its popover.",
          "type": "object",
          "properties": {
            "extends": {
              "type": "string",
              "const": "audio"
            }
          },
          "additionalProperties": false,
          "required": []
        },
        "battery": {
          "description": "Charge level and time remaining, with the power profile in its popover.",
          "type": "object",
          "properties": {
            "extends": {
              "type": "string",
              "const": "battery"
            }
          },
          "additionalProperties": false,
          "required": []
        },
        "bluetooth": {
          "description": "Adapter state and paired devices.",
          "type": "object",
          "properties": {
            "extends": {
              "type": "string",
              "const": "bluetooth"
            }
          },
          "additionalProperties": false,
          "required": []
        },
        "brightness": {
          "description": "Display backlight level.",
          "type": "object",
          "properties": {
            "extends": {
              "type": "string",
              "const": "brightness"
            }
          },
          "additionalProperties": false,
          "required": []
        },
        "clipboard": {
          "description": "Clipboard history.",
          "type": "object",
          "properties": {
            "extends": {
              "type": "string",
              "const": "clipboard"
            }
          },
          "additionalProperties": false,
          "required": []
        },
        "clock": {
          "description": "The time and date, with a calendar in its popover.",
          "type": "object",
          "properties": {
            "extends": {
              "type": "string",
              "const": "clock"
            },
            "format": {
              "description": "A `strftime` format string, such as `%H:%M` or `%a %d %b %H:%M`.",
              "type": "string",
              "default": "%H:%M"
            }
          },
          "additionalProperties": false,
          "required": []
        },
        "command": {
          "description": "Runs a command and renders its output on the bar.",
          "type": "object",
          "properties": {
            "extends": {
              "type": "string",
              "const": "command"
            }
          },
          "additionalProperties": false,
          "required": []
        },
        "display": {
          "description": "Connected outputs, their modes and their arrangement.",
          "type": "object",
          "properties": {
            "extends": {
              "type": "string",
              "const": "display"
            }
          },
          "additionalProperties": false,
          "required": []
        },
        "exec": {
          "description": "Hosts a third-party applet binary that draws its own popover.",
          "type": "object",
          "properties": {
            "extends": {
              "type": "string",
              "const": "exec"
            }
          },
          "additionalProperties": false,
          "required": []
        },
        "heartbeat": {
          "description": "A counter that ticks once a second. A development fixture: it proves the daemon is\nreachable and events are arriving, and is not meant for a real bar.",
          "type": "object",
          "properties": {
            "extends": {
              "type": "string",
              "const": "heartbeat"
            }
          },
          "additionalProperties": false,
          "required": []
        },
        "idle": {
          "description": "Idle inhibition, for keeping the screen awake.",
          "type": "object",
          "properties": {
            "extends": {
              "type": "string",
              "const": "idle"
            }
          },
          "additionalProperties": false,
          "required": []
        },
        "keyboard": {
          "description": "The active keyboard layout, and switches between the configured ones.",
          "type": "object",
          "properties": {
            "extends": {
              "type": "string",
              "const": "keyboard"
            }
          },
          "additionalProperties": false,
          "required": []
        },
        "mpris": {
          "description": "The currently playing track, with transport controls in its popover.",
          "type": "object",
          "properties": {
            "extends": {
              "type": "string",
              "const": "mpris"
            }
          },
          "additionalProperties": false,
          "required": []
        },
        "network": {
          "description": "Connection state, with the available networks in its popover.",
          "type": "object",
          "properties": {
            "extends": {
              "type": "string",
              "const": "network"
            }
          },
          "additionalProperties": false,
          "required": []
        },
        "next-event": {
          "description": "The next entry from the configured calendars.",
          "type": "object",
          "properties": {
            "extends": {
              "type": "string",
              "const": "next-event"
            }
          },
          "additionalProperties": false,
          "required": []
        },
        "notifications": {
          "description": "Unread notifications, with their history in its popover.",
          "type": "object",
          "properties": {
            "extends": {
              "type": "string",
              "const": "notifications"
            }
          },
          "additionalProperties": false,
          "required": []
        },
        "pager": {
          "description": "A strip of workspaces or windows, one slot each, that switches between them on a click.",
          "type": "object",
          "properties": {
            "extends": {
              "type": "string",
              "const": "pager"
            },
            "focused-label": {
              "description": "The label for the slot the user is on, so the current workspace can show its name while\nthe rest stay numbers. Same tokens as `label`; unset falls back to it.",
              "type": [
                "string",
                "null"
              ]
            },
            "label": {
              "description": "What a slot reads, when the shape is `labels`, and the fallback for every state below.\nUnderstands `{index}`, `{id}`, `{name}`, `{name-or-index}` and `{workspace-name}`.\n`{index}` falls back to the id, because only niri numbers its workspaces separately from\ntheir ids; in `windows` mode it is the slot's position and `{name}` is the window's app\nid, which is why `{workspace-name}` exists.",
              "type": "string",
              "default": "{index}"
            },
            "mode": {
              "description": "Whether each slot is a workspace or a window.",
              "$ref": "#/$defs/PagerMode",
              "default": "workspaces"
            },
            "scope": {
              "description": "How much of the session the strip covers.",
              "$ref": "#/$defs/PagerScope",
              "default": "output"
            },
            "shape": {
              "description": "Whether a slot is drawn as a dot or as its label in a pill.",
              "$ref": "#/$defs/PagerShape",
              "default": "dots"
            },
            "unfocused-label": {
              "description": "The label for every slot the user is not on. Same tokens as `label`; unset falls back\nto it.",
              "type": [
                "string",
                "null"
              ]
            },
            "urgent-label": {
              "description": "The label for a slot asking for attention. Takes precedence over the other two, because a\nwindow wanting attention is the one thing the strip exists to surface. Same tokens as\n`label`; unset falls back to it.",
              "type": [
                "string",
                "null"
              ]
            }
          },
          "additionalProperties": false,
          "required": []
        },
        "printing": {
          "description": "Active print jobs.",
          "type": "object",
          "properties": {
            "extends": {
              "type": "string",
              "const": "printing"
            }
          },
          "additionalProperties": false,
          "required": []
        },
        "privacy": {
          "description": "Shows when the microphone, camera or screen is in use.",
          "type": "object",
          "properties": {
            "extends": {
              "type": "string",
              "const": "privacy"
            }
          },
          "additionalProperties": false,
          "required": []
        },
        "removable": {
          "description": "Mounted removable drives, and unmounts them.",
          "type": "object",
          "properties": {
            "extends": {
              "type": "string",
              "const": "removable"
            }
          },
          "additionalProperties": false,
          "required": []
        },
        "session": {
          "description": "Log out, suspend, restart and shut down.",
          "type": "object",
          "properties": {
            "extends": {
              "type": "string",
              "const": "session"
            }
          },
          "additionalProperties": false,
          "required": []
        },
        "tray": {
          "description": "The system tray: icons from applications that ask for one.",
          "type": "object",
          "properties": {
            "extends": {
              "type": "string",
              "const": "tray"
            }
          },
          "additionalProperties": false,
          "required": []
        },
        "weather": {
          "description": "Current conditions, with the forecast in its popover.",
          "type": "object",
          "properties": {
            "extends": {
              "type": "string",
              "const": "weather"
            }
          },
          "additionalProperties": false,
          "required": []
        }
      },
      "additionalProperties": {
        "description": "One applet on a bar. The table name is the applet's name, and `extends` says which kind it is\nwhen the two differ — which is how one kind can appear more than once, as `[applets.clock-utc]`\nwith `extends = \"clock\"`.",
        "oneOf": [
          {
            "description": "Output volume, with the default sink and per-application streams in its popover.",
            "type": "object",
            "properties": {
              "extends": {
                "type": "string",
                "const": "audio"
              }
            },
            "additionalProperties": false,
            "required": [
              "extends"
            ]
          },
          {
            "description": "Charge level and time remaining, with the power profile in its popover.",
            "type": "object",
            "properties": {
              "extends": {
                "type": "string",
                "const": "battery"
              }
            },
            "additionalProperties": false,
            "required": [
              "extends"
            ]
          },
          {
            "description": "Adapter state and paired devices.",
            "type": "object",
            "properties": {
              "extends": {
                "type": "string",
                "const": "bluetooth"
              }
            },
            "additionalProperties": false,
            "required": [
              "extends"
            ]
          },
          {
            "description": "Display backlight level.",
            "type": "object",
            "properties": {
              "extends": {
                "type": "string",
                "const": "brightness"
              }
            },
            "additionalProperties": false,
            "required": [
              "extends"
            ]
          },
          {
            "description": "Clipboard history.",
            "type": "object",
            "properties": {
              "extends": {
                "type": "string",
                "const": "clipboard"
              }
            },
            "additionalProperties": false,
            "required": [
              "extends"
            ]
          },
          {
            "description": "The time and date, with a calendar in its popover.",
            "type": "object",
            "properties": {
              "extends": {
                "type": "string",
                "const": "clock"
              },
              "format": {
                "description": "A `strftime` format string, such as `%H:%M` or `%a %d %b %H:%M`.",
                "type": "string",
                "default": "%H:%M"
              }
            },
            "additionalProperties": false,
            "required": [
              "extends"
            ]
          },
          {
            "description": "Runs a command and renders its output on the bar.",
            "type": "object",
            "properties": {
              "extends": {
                "type": "string",
                "const": "command"
              }
            },
            "additionalProperties": false,
            "required": [
              "extends"
            ]
          },
          {
            "description": "Connected outputs, their modes and their arrangement.",
            "type": "object",
            "properties": {
              "extends": {
                "type": "string",
                "const": "display"
              }
            },
            "additionalProperties": false,
            "required": [
              "extends"
            ]
          },
          {
            "description": "Hosts a third-party applet binary that draws its own popover.",
            "type": "object",
            "properties": {
              "extends": {
                "type": "string",
                "const": "exec"
              }
            },
            "additionalProperties": false,
            "required": [
              "extends"
            ]
          },
          {
            "description": "A counter that ticks once a second. A development fixture: it proves the daemon is\nreachable and events are arriving, and is not meant for a real bar.",
            "type": "object",
            "properties": {
              "extends": {
                "type": "string",
                "const": "heartbeat"
              }
            },
            "additionalProperties": false,
            "required": [
              "extends"
            ]
          },
          {
            "description": "Idle inhibition, for keeping the screen awake.",
            "type": "object",
            "properties": {
              "extends": {
                "type": "string",
                "const": "idle"
              }
            },
            "additionalProperties": false,
            "required": [
              "extends"
            ]
          },
          {
            "description": "The active keyboard layout, and switches between the configured ones.",
            "type": "object",
            "properties": {
              "extends": {
                "type": "string",
                "const": "keyboard"
              }
            },
            "additionalProperties": false,
            "required": [
              "extends"
            ]
          },
          {
            "description": "The currently playing track, with transport controls in its popover.",
            "type": "object",
            "properties": {
              "extends": {
                "type": "string",
                "const": "mpris"
              }
            },
            "additionalProperties": false,
            "required": [
              "extends"
            ]
          },
          {
            "description": "Connection state, with the available networks in its popover.",
            "type": "object",
            "properties": {
              "extends": {
                "type": "string",
                "const": "network"
              }
            },
            "additionalProperties": false,
            "required": [
              "extends"
            ]
          },
          {
            "description": "The next entry from the configured calendars.",
            "type": "object",
            "properties": {
              "extends": {
                "type": "string",
                "const": "next-event"
              }
            },
            "additionalProperties": false,
            "required": [
              "extends"
            ]
          },
          {
            "description": "Unread notifications, with their history in its popover.",
            "type": "object",
            "properties": {
              "extends": {
                "type": "string",
                "const": "notifications"
              }
            },
            "additionalProperties": false,
            "required": [
              "extends"
            ]
          },
          {
            "description": "A strip of workspaces or windows, one slot each, that switches between them on a click.",
            "type": "object",
            "properties": {
              "extends": {
                "type": "string",
                "const": "pager"
              },
              "focused-label": {
                "description": "The label for the slot the user is on, so the current workspace can show its name while\nthe rest stay numbers. Same tokens as `label`; unset falls back to it.",
                "type": [
                  "string",
                  "null"
                ]
              },
              "label": {
                "description": "What a slot reads, when the shape is `labels`, and the fallback for every state below.\nUnderstands `{index}`, `{id}`, `{name}`, `{name-or-index}` and `{workspace-name}`.\n`{index}` falls back to the id, because only niri numbers its workspaces separately from\ntheir ids; in `windows` mode it is the slot's position and `{name}` is the window's app\nid, which is why `{workspace-name}` exists.",
                "type": "string",
                "default": "{index}"
              },
              "mode": {
                "description": "Whether each slot is a workspace or a window.",
                "$ref": "#/$defs/PagerMode",
                "default": "workspaces"
              },
              "scope": {
                "description": "How much of the session the strip covers.",
                "$ref": "#/$defs/PagerScope",
                "default": "output"
              },
              "shape": {
                "description": "Whether a slot is drawn as a dot or as its label in a pill.",
                "$ref": "#/$defs/PagerShape",
                "default": "dots"
              },
              "unfocused-label": {
                "description": "The label for every slot the user is not on. Same tokens as `label`; unset falls back\nto it.",
                "type": [
                  "string",
                  "null"
                ]
              },
              "urgent-label": {
                "description": "The label for a slot asking for attention. Takes precedence over the other two, because a\nwindow wanting attention is the one thing the strip exists to surface. Same tokens as\n`label`; unset falls back to it.",
                "type": [
                  "string",
                  "null"
                ]
              }
            },
            "additionalProperties": false,
            "required": [
              "extends"
            ]
          },
          {
            "description": "Active print jobs.",
            "type": "object",
            "properties": {
              "extends": {
                "type": "string",
                "const": "printing"
              }
            },
            "additionalProperties": false,
            "required": [
              "extends"
            ]
          },
          {
            "description": "Shows when the microphone, camera or screen is in use.",
            "type": "object",
            "properties": {
              "extends": {
                "type": "string",
                "const": "privacy"
              }
            },
            "additionalProperties": false,
            "required": [
              "extends"
            ]
          },
          {
            "description": "Mounted removable drives, and unmounts them.",
            "type": "object",
            "properties": {
              "extends": {
                "type": "string",
                "const": "removable"
              }
            },
            "additionalProperties": false,
            "required": [
              "extends"
            ]
          },
          {
            "description": "Log out, suspend, restart and shut down.",
            "type": "object",
            "properties": {
              "extends": {
                "type": "string",
                "const": "session"
              }
            },
            "additionalProperties": false,
            "required": [
              "extends"
            ]
          },
          {
            "description": "The system tray: icons from applications that ask for one.",
            "type": "object",
            "properties": {
              "extends": {
                "type": "string",
                "const": "tray"
              }
            },
            "additionalProperties": false,
            "required": [
              "extends"
            ]
          },
          {
            "description": "Current conditions, with the forecast in its popover.",
            "type": "object",
            "properties": {
              "extends": {
                "type": "string",
                "const": "weather"
              }
            },
            "additionalProperties": false,
            "required": [
              "extends"
            ]
          }
        ]
      },
      "default": {}
    },
    "backdrop": {
      "$ref": "#/$defs/Backdrop",
      "default": {
        "blur-radius": 24,
        "enabled": true
      }
    },
    "calendar": {
      "$ref": "#/$defs/Calendar",
      "default": {
        "poll-interval": 600,
        "sources": []
      }
    },
    "geolocation": {
      "$ref": "#/$defs/Geolocation",
      "default": {
        "provider": "geoclue"
      }
    },
    "idle": {
      "$ref": "#/$defs/Idle",
      "default": {
        "enabled": true,
        "profiles": {
          "ac": {
            "listeners": [
              {
                "on-idle": "/usr/share/glimpse/scripts/monitors off",
                "on-resume": "/usr/share/glimpse/scripts/monitors on",
                "timeout": 600
              },
              {
                "on-idle": "loginctl lock-session",
                "on-resume": "",
                "timeout": 900
              },
              {
                "on-idle": "systemctl suspend",
                "on-resume": "",
                "timeout": 3600
              }
            ]
          },
          "battery": {
            "listeners": [
              {
                "on-idle": "/usr/share/glimpse/scripts/monitors off",
                "on-resume": "/usr/share/glimpse/scripts/monitors on",
                "timeout": 300
              },
              {
                "on-idle": "loginctl lock-session",
                "on-resume": "",
                "timeout": 900
              },
              {
                "on-idle": "systemctl suspend",
                "on-resume": "",
                "timeout": 1800
              }
            ]
          }
        },
        "respect-inhibitors": true
      }
    },
    "keyboard": {
      "$ref": "#/$defs/Keyboard",
      "default": {
        "labels": {},
        "remember": "window"
      }
    },
    "lock": {
      "$ref": "#/$defs/Lock",
      "default": {
        "background": "",
        "background-dark": "",
        "clock": {
          "date-format": "%A, %B %-d",
          "enabled": true,
          "time-format": "%H:%M"
        },
        "controls": {
          "buttons": [
            "wifi",
            "input",
            "weather",
            "battery",
            "power"
          ]
        },
        "dim": 1.0,
        "dim-dark": 1.0,
        "pam-service": "glimpse-lock"
      }
    },
    "monitors": {
      "$ref": "#/$defs/Monitors",
      "default": {}
    },
    "night-light": {
      "$ref": "#/$defs/NightLight",
      "default": {
        "schedule": "automatic",
        "temperature": 4200,
        "transition-minutes": 15
      }
    },
    "panels": {
      "type": "array",
      "default": [
        {
          "center": [
            "clock",
            "weather",
            "notifications",
            "privacy"
          ],
          "left": [
            "pager",
            "mpris"
          ],
          "margin": {
            "bottom": 0,
            "left": 0,
            "right": 0,
            "top": 0
          },
          "position": "top",
          "right": [
            "next-event",
            "tray",
            "removable",
            "clipboard",
            "keyboard",
            "printing",
            "bluetooth",
            "network",
            "display",
            "audio",
            "idle",
            "battery",
            "session"
          ],
          "size": 36
        }
      ],
      "items": {
        "$ref": "#/$defs/Panel"
      }
    },
    "power": {
      "$ref": "#/$defs/Power",
      "default": {
        "lock-before-sleep": true,
        "lock-on-request": true
      }
    },
    "wallpaper": {
      "$ref": "#/$defs/Wallpaper",
      "default": {
        "background": "",
        "background-dark": "",
        "color": "#000000",
        "fit": "fill"
      }
    }
  },
  "additionalProperties": false,
  "$defs": {
    "Appearance": {
      "type": "object",
      "properties": {
        "color-scheme": {
          "$ref": "#/$defs/ColorScheme",
          "default": "auto"
        },
        "theme": {
          "type": "string",
          "default": "adwaita"
        },
        "theme-variant": {
          "type": "string",
          "default": ""
        }
      },
      "additionalProperties": false
    },
    "Backdrop": {
      "type": "object",
      "properties": {
        "blur-radius": {
          "type": "integer",
          "format": "uint32",
          "default": 24,
          "minimum": 0
        },
        "enabled": {
          "type": "boolean",
          "default": true
        },
        "path": {
          "type": [
            "string",
            "null"
          ]
        }
      },
      "additionalProperties": false
    },
    "Button": {
      "type": "string",
      "enum": [
        "wifi",
        "input",
        "weather",
        "battery",
        "power"
      ]
    },
    "Calendar": {
      "type": "object",
      "properties": {
        "poll-interval": {
          "type": "integer",
          "format": "uint64",
          "default": 600,
          "minimum": 0
        },
        "sources": {
          "type": "array",
          "default": [],
          "items": {
            "$ref": "#/$defs/Source"
          }
        }
      },
      "additionalProperties": false
    },
    "Clock": {
      "type": "object",
      "properties": {
        "date-format": {
          "type": "string",
          "default": "%A, %B %-d"
        },
        "enabled": {
          "type": "boolean",
          "default": true
        },
        "time-format": {
          "type": "string",
          "default": "%H:%M"
        }
      },
      "additionalProperties": false
    },
    "ColorScheme": {
      "type": "string",
      "enum": [
        "light",
        "dark",
        "auto"
      ]
    },
    "Controls": {
      "type": "object",
      "properties": {
        "buttons": {
          "type": "array",
          "default": [
            "wifi",
            "input",
            "weather",
            "battery",
            "power"
          ],
          "items": {
            "$ref": "#/$defs/Button"
          }
        }
      },
      "additionalProperties": false
    },
    "Fit": {
      "type": "string",
      "enum": [
        "cover",
        "contain",
        "fill"
      ]
    },
    "Geolocation": {
      "description": "`manual` carries its coordinates in the table that selects it, so a provider without them cannot\nbe written down: serde refuses the document and names the key that is missing. The alternative —\ntwo loose `Option` fields — makes a half-filled table a runtime problem for every reader.",
      "oneOf": [
        {
          "type": "object",
          "properties": {
            "provider": {
              "type": "string",
              "const": "geoclue"
            }
          },
          "required": [
            "provider"
          ]
        },
        {
          "type": "object",
          "properties": {
            "latitude": {
              "type": "number",
              "format": "double"
            },
            "longitude": {
              "type": "number",
              "format": "double"
            },
            "provider": {
              "type": "string",
              "const": "manual"
            }
          },
          "required": [
            "provider",
            "latitude",
            "longitude"
          ]
        }
      ]
    },
    "Idle": {
      "type": "object",
      "properties": {
        "enabled": {
          "type": "boolean",
          "default": true
        },
        "profiles": {
          "$ref": "#/$defs/Profiles",
          "default": {
            "ac": {
              "listeners": [
                {
                  "on-idle": "/usr/share/glimpse/scripts/monitors off",
                  "on-resume": "/usr/share/glimpse/scripts/monitors on",
                  "timeout": 600
                },
                {
                  "on-idle": "loginctl lock-session",
                  "on-resume": "",
                  "timeout": 900
                },
                {
                  "on-idle": "systemctl suspend",
                  "on-resume": "",
                  "timeout": 3600
                }
              ]
            },
            "battery": {
              "listeners": [
                {
                  "on-idle": "/usr/share/glimpse/scripts/monitors off",
                  "on-resume": "/usr/share/glimpse/scripts/monitors on",
                  "timeout": 300
                },
                {
                  "on-idle": "loginctl lock-session",
                  "on-resume": "",
                  "timeout": 900
                },
                {
                  "on-idle": "systemctl suspend",
                  "on-resume": "",
                  "timeout": 1800
                }
              ]
            }
          }
        },
        "respect-inhibitors": {
          "type": "boolean",
          "default": true
        }
      },
      "additionalProperties": false
    },
    "Keyboard": {
      "type": "object",
      "properties": {
        "labels": {
          "type": "object",
          "additionalProperties": {
            "type": "string"
          },
          "default": {}
        },
        "remember": {
          "$ref": "#/$defs/Remember",
          "default": "window"
        }
      },
      "additionalProperties": false
    },
    "Listener": {
      "type": "object",
      "properties": {
        "on-idle": {
          "type": "string",
          "default": ""
        },
        "on-resume": {
          "type": "string",
          "default": ""
        },
        "respect-inhibitors": {
          "type": [
            "boolean",
            "null"
          ]
        },
        "timeout": {
          "type": "integer",
          "format": "uint64",
          "default": 0,
          "minimum": 0
        }
      },
      "additionalProperties": false
    },
    "Lock": {
      "type": "object",
      "properties": {
        "background": {
          "type": "string",
          "default": ""
        },
        "background-dark": {
          "type": "string",
          "default": ""
        },
        "clock": {
          "$ref": "#/$defs/Clock",
          "default": {
            "date-format": "%A, %B %-d",
            "enabled": true,
            "time-format": "%H:%M"
          }
        },
        "controls": {
          "$ref": "#/$defs/Controls",
          "default": {
            "buttons": [
              "wifi",
              "input",
              "weather",
              "battery",
              "power"
            ]
          }
        },
        "dim": {
          "type": "number",
          "format": "float",
          "default": 1.0
        },
        "dim-dark": {
          "type": "number",
          "format": "float",
          "default": 1.0
        },
        "pam-service": {
          "type": "string",
          "default": "glimpse-lock"
        }
      },
      "additionalProperties": false
    },
    "Margin": {
      "type": "object",
      "properties": {
        "bottom": {
          "type": "integer",
          "format": "uint32",
          "default": 0,
          "minimum": 0
        },
        "left": {
          "type": "integer",
          "format": "uint32",
          "default": 0,
          "minimum": 0
        },
        "right": {
          "type": "integer",
          "format": "uint32",
          "default": 0,
          "minimum": 0
        },
        "top": {
          "type": "integer",
          "format": "uint32",
          "default": 0,
          "minimum": 0
        }
      },
      "additionalProperties": false
    },
    "Monitors": {
      "type": "object",
      "properties": {
        "builtin-connector": {
          "type": [
            "string",
            "null"
          ]
        }
      },
      "additionalProperties": false
    },
    "NightLight": {
      "type": "object",
      "properties": {
        "end-time": {
          "type": [
            "string",
            "null"
          ]
        },
        "schedule": {
          "$ref": "#/$defs/Schedule",
          "default": "automatic"
        },
        "start-time": {
          "type": [
            "string",
            "null"
          ]
        },
        "temperature": {
          "type": "integer",
          "format": "uint32",
          "default": 4200,
          "minimum": 0
        },
        "transition-minutes": {
          "type": "integer",
          "format": "uint32",
          "default": 15,
          "minimum": 0
        }
      },
      "additionalProperties": false
    },
    "PagerMode": {
      "description": "What each slot stands for: `workspaces` for one slot per workspace, `windows` for one slot per\nwindow on the current workspace.",
      "type": "string",
      "enum": [
        "workspaces",
        "windows"
      ]
    },
    "PagerScope": {
      "description": "How much of the session the strip covers: `current` for only the workspace the user is on,\n`output` for the workspaces on this panel's monitor, `session` for every workspace on every\nmonitor.",
      "type": "string",
      "enum": [
        "current",
        "output",
        "session"
      ]
    },
    "PagerShape": {
      "description": "How a slot is drawn: `dots` for a dot each with the current one drawn longer, which takes\nthe least room on the bar, or `labels` for the slot's label in a pill.",
      "type": "string",
      "enum": [
        "dots",
        "labels"
      ]
    },
    "Panel": {
      "type": "object",
      "properties": {
        "center": {
          "type": "array",
          "default": [
            "clock",
            "weather",
            "notifications",
            "privacy"
          ],
          "items": {
            "type": "string"
          }
        },
        "left": {
          "type": "array",
          "default": [
            "pager",
            "mpris"
          ],
          "items": {
            "type": "string"
          }
        },
        "margin": {
          "$ref": "#/$defs/Margin",
          "default": {
            "bottom": 0,
            "left": 0,
            "right": 0,
            "top": 0
          }
        },
        "monitor": {
          "type": [
            "string",
            "null"
          ]
        },
        "position": {
          "$ref": "#/$defs/Position",
          "default": "top"
        },
        "right": {
          "type": "array",
          "default": [
            "next-event",
            "tray",
            "removable",
            "clipboard",
            "keyboard",
            "printing",
            "bluetooth",
            "network",
            "display",
            "audio",
            "idle",
            "battery",
            "session"
          ],
          "items": {
            "type": "string"
          }
        },
        "size": {
          "type": "integer",
          "format": "uint32",
          "default": 36,
          "minimum": 0
        }
      },
      "additionalProperties": false
    },
    "Position": {
      "type": "string",
      "enum": [
        "left",
        "top",
        "right",
        "bottom"
      ]
    },
    "Power": {
      "type": "object",
      "properties": {
        "lock-before-sleep": {
          "type": "boolean",
          "default": true
        },
        "lock-on-request": {
          "type": "boolean",
          "default": true
        }
      },
      "additionalProperties": false
    },
    "Profile": {
      "type": "object",
      "properties": {
        "listeners": {
          "type": "array",
          "default": [],
          "items": {
            "$ref": "#/$defs/Listener"
          }
        }
      },
      "additionalProperties": false
    },
    "Profiles": {
      "type": "object",
      "properties": {
        "ac": {
          "$ref": "#/$defs/Profile",
          "default": {
            "listeners": [
              {
                "on-idle": "/usr/share/glimpse/scripts/monitors off",
                "on-resume": "/usr/share/glimpse/scripts/monitors on",
                "timeout": 600
              },
              {
                "on-idle": "loginctl lock-session",
                "on-resume": "",
                "timeout": 900
              },
              {
                "on-idle": "systemctl suspend",
                "on-resume": "",
                "timeout": 3600
              }
            ]
          }
        },
        "battery": {
          "$ref": "#/$defs/Profile",
          "default": {
            "listeners": [
              {
                "on-idle": "/usr/share/glimpse/scripts/monitors off",
                "on-resume": "/usr/share/glimpse/scripts/monitors on",
                "timeout": 300
              },
              {
                "on-idle": "loginctl lock-session",
                "on-resume": "",
                "timeout": 900
              },
              {
                "on-idle": "systemctl suspend",
                "on-resume": "",
                "timeout": 1800
              }
            ]
          }
        }
      },
      "additionalProperties": false
    },
    "Remember": {
      "type": "string",
      "enum": [
        "global",
        "app",
        "window"
      ]
    },
    "Schedule": {
      "type": "string",
      "enum": [
        "off",
        "automatic",
        "schedule"
      ]
    },
    "Source": {
      "type": "object",
      "properties": {
        "color": {
          "type": [
            "string",
            "null"
          ]
        },
        "id": {
          "type": "string"
        },
        "name": {
          "type": [
            "string",
            "null"
          ]
        },
        "poll-interval": {
          "type": [
            "integer",
            "null"
          ],
          "format": "uint64",
          "minimum": 0
        },
        "type": {
          "$ref": "#/$defs/SourceKind"
        },
        "uri": {
          "type": "string"
        }
      },
      "additionalProperties": false,
      "required": [
        "id",
        "type",
        "uri"
      ]
    },
    "SourceKind": {
      "type": "string",
      "enum": [
        "ical",
        "directory"
      ]
    },
    "Wallpaper": {
      "type": "object",
      "properties": {
        "background": {
          "type": "string",
          "default": ""
        },
        "background-dark": {
          "type": "string",
          "default": ""
        },
        "color": {
          "type": "string",
          "default": "#000000"
        },
        "fit": {
          "$ref": "#/$defs/Fit",
          "default": "fill"
        }
      },
      "additionalProperties": false
    }
  }
}
