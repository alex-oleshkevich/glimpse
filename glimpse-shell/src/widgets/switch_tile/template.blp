using Gtk 4.0;

template $GlimpseSwitchTile: Box {
  orientation: horizontal;
  spacing: 8;

  styles [
    "tile",
    "switch-tile",
  ]

  Box left_slot {
    valign: center;
    visible: false;

    styles [
      "tile__left",
    ]
  }

  Box labels {
    orientation: vertical;
    hexpand: true;
    valign: center;

    styles [
      "tile__labels",
    ]

    Label primary_label {
      xalign: 0;
      ellipsize: end;
      hexpand: true;

      styles [
        "tile__label_primary",
      ]
    }

    Label secondary_label {
      xalign: 0;
      ellipsize: end;
      visible: false;

      styles [
        "dim-label",
        "caption",
        "tile__label_secondary",
      ]
    }
  }

  Switch toggle {
    valign: center;
  }
}
