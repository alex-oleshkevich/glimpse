using Gtk 4.0;

template $ColorPickerPopover: Gtk.Widget {
  layout-manager: BinLayout {};

  styles [
    "color-picker-popover",
  ]

  $PopoverShell shell {
    [hero]
    $Hero hero {
      icon-name: "color-select-symbolic";
      title: _("Color picker");

      [slot]
      $Swatch latest {
        visible: false;
        valign: center;

        styles [
          "swatch--hero",
        ]
      }
    }

    Gtk.Box column {
      orientation: vertical;
      hexpand: true;

      styles [
        "column",
      ]

      $Row pick {
        title: _("Pick a color");
        lead-icon: "color-select-symbolic";
      }

      $Section palette_section {
        visible: false;
        title: _("Palette");

        $ColorList palette {}
      }
    }

    [footer]
    $Row footer {
      visible: false;
    }
  }
}
